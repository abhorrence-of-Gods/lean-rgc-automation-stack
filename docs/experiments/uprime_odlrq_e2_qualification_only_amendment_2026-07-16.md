# U-prime / ODLRQ E2 qualification-only re-entry amendment

Date: 2026-07-16 (Asia/Tokyo)

Status: **BYTES FROZEN WHEN THIS ONE DOCUMENT-ONLY COMMIT IS COMMITTED AND
PUSHED; EXECUTION LICENSE ACTIVE ONLY AFTER THE EXACT AUTHORITY CI GATE BELOW.**
This is a fresh execution authority for one Windows-CPU qualification of an
already immutable E2 semantic commit.  It is not a new E2 construction phase,
not a mathematical re-review, not a runner repair, and not permission to edit
the E2 source after observing a test outcome.

## 1. Why this re-entry exists

Accepted E1 remains:

```text
commit  6fb35aa229fc60e2220cbb68c1e7fff2ce64f199
tree    b3fc7f21b6420e718eb954be0c1b5affca65d263
subject uprime: qualify exact E1 fiber envelope
ref     codex/uprime-odlrq-plan
```

The E2 endpoint definitions were frozen by the semantic-definition authority:

```text
commit        28c5a29000dddadcaf3e9ad9dd5534554dd67f32
tree          1a71fc6ff774dd0bcf7e4ab551bd737a7a9dab14
document      docs/experiments/uprime_odlrq_e2_endpoint_semantics_authority_amendment_2026-07-16.md
document blob 139a5992a38269974068858ef00f47f43ef5fca4
```

That authority uniquely fixed parent-envelope restriction, the retained/memory
split, weighted norm and block orientation, and the complete pre-gate candidate
universe.  Those definitions are adopted byte-for-byte and are not reopened.

Its official runner carrier `bad1114174c05dba24311e7be219bb11692e61c1`
failed in the first suspended Git setup, before Resume, topology observation,
manifest creation, marker creation, Python, import, collection, or any E2 node.
The terminal failure closeout is:

```text
ref           codex/uprime-e2-endpoint-failure-closeout
commit        4445059760b9ec4eac85dc00ad5666c1f4fc699d
tree          8b4961da091dc75bf0c5bc3e66d2c5b741765f99
document blob f1a579f9e23c645ad5bfa998d52c53b242aa5f7c
disposition   U24_E2_RUNNER_BLOCKED
operator invocations 1
scientific execution count 0
```

The exact closeout CI was the preregistered topology-control red: run
`29500783442`, job `87628862349`, with `2599 passed, 1 failed, 8 skipped,
161 deselected`, the sole failed node
`tests/test_uprime_u2_u4_development.py::test_u24_b0_anchor_contiguous_budget_and_terminal_topology`,
and zero E2 module/node hits.  No correction or rerun occurred.

The old authority is therefore closed correctly, but it produced no E2 result.
This document is a distinct execution authority.  The constants naming
`28c5a290...` inside the E2 files remain provenance for the unchanged semantic
definition; they are not changed or relabeled as this execution authority.

The fresh authority comes from the user's direct 2026-07-16 instruction to
continue autonomously after the terminal closeout and to treat Fable as
advisory unless it supplies a trajectory correction.  The old operator
invocation remains permanently spent.  Q0 is the sole qualification-only
re-entry licensed by that instruction.  If Q0 fails, Codex or Fable cannot
self-authorize an isomorphic Q1; reopening would require a new explicit user
decision after the Q0 closeout.

## 2. Completed kill gates and actual next dependency

U05-KP1, U05-KP2, and U05-KP3 have already consumed their registered look and
closed respectively as `U05_KP1_SCALE_READY`, `U05_KP2_EVENTUAL_WINDOW`, and
`U05_KP3_PLATEAU_AT_D3`.  WP4-E, WP4-H, WP4-W, and WP6-T are already verified
development assets.  The fresh-family KP3 depth-four implementation exists but
its registered scientific run closed `D4_EXECUTION_FAILED`; that family is not
rerun here.

Repeating any of those probes would add no information and would violate their
one-look boundary.  The next dependency in the registered upper-stack order is
qualification of the frozen E2 endpoint before ME0 MaxEnt, S0 similarity, I0
integration, or the locality learner can begin.

## 3. Immutable scientific candidate

The sole candidate is the existing direct child of accepted E1:

```text
commit  6998f2f9ec430881df50e6790ef9a8f13b1b7857
tree    3512b6bc2e7e357544f87f2e7e05e8868b26d658
parent  6fb35aa229fc60e2220cbb68c1e7fff2ce64f199
subject uprime: qualify exact E2 endpoint
```

It changes exactly four mode-`100644` paths relative to E1:

| path | Git blob |
|---|---|
| `lean_rgc/odlrq/certificates.py` | `8d995768e93da62829035a1ff187a74e3ea8a378` |
| `lean_rgc/odlrq/selection.py` | `b856555271bdea8eb9f9f05e41b4aa52cab9c95d` |
| `tests/test_odlrq_selection.py` | `e22223008cab410dd99f953806ce18d38db854f6` |
| `tests/tier_manifest.json` | `c649f7f3d74b8a08ea880e250fc8583a21eef790` |

The candidate is immutable.  This phase permits zero semantic commits, zero
semantic corrections, zero manifest changes, and zero test changes.  A byte,
mode, tree, parent, subject, or path mismatch stops before execution.

This authority sidecar is a document-only child of closeout `4445059` so its
date and governance order are unambiguous.  It is never merged into the
scientific candidate or accepted line.  Its fixed ref is
`codex/uprime-e2-qualification-authority-q0`.

The authority commit is frozen as follows:

```text
sole parent  4445059760b9ec4eac85dc00ad5666c1f4fc699d
changed path docs/experiments/uprime_odlrq_e2_qualification_only_amendment_2026-07-16.md
path mode    100644
subject      uprime: freeze E2 qualification-only re-entry
remote ref   codex/uprime-e2-qualification-authority-q0
publication  one non-force push; no amend, rebase, repoint, delete, or force
```

Its natural push CI must finish before a qualification worktree is created or
the Windows invocation begins.  The only admissible authority-side observation
is push event, run attempt 1, exact authority head SHA, exactly `2599 passed,
1 failed, 8 skipped, 161 deselected`, the sole failed node
`tests/test_uprime_u2_u4_development.py::test_u24_b0_anchor_contiguous_budget_and_terminal_topology`,
and zero `test_odlrq_selection.py` or `::test_e2_` hit.  An extra failure, an E2
hit, wrong event/SHA/count, missing run, or Actions rerun stops Q0 before local
execution.  The known red is control topology, not an E2 result.

## 4. Exact Windows-CPU qualification

The qualification uses a clean detached worktree at candidate `6998f2f` and
the following already installed interpreter identity:

```text
executable  C:\Python313\python.exe
version     Python 3.13.7
SHA-256     D932E5E2F324D57F392E8FD063DCF6D0185BE8A664C57C6D24E7762ED02C28CA
pytest      9.0.3
numpy       2.3.3 (loaded only by the inherited package import chain)
```

Before setting the three admitted values, the control block removes
`PYTEST_ADDOPTS`, `PYTEST_PLUGINS`, `PYTHONPATH`, `PYTHONHOME`,
`PYTHONNOUSERSITE`, `PYTHONUSERBASE`, and `PYTHONWARNINGS`.  The resulting
invocation-specific Python/pytest environment sets only:

```text
PYTEST_DISABLE_PLUGIN_AUTOLOAD=1
PYTHONDONTWRITEBYTECODE=1
PYTHONHASHSEED=0
```

After the ordered preflight below, it performs one standard child invocation
of `C:\Python313\python.exe -m pytest -q` with the following ten full node IDs
in this exact order:

```text
tests/test_odlrq_selection.py::test_e2_square_parent_coordinate_identification_rederives_complete_typed_basis
tests/test_odlrq_selection.py::test_e2_restriction_replays_full_parent_terminal_zeros_and_restricted_weights
tests/test_odlrq_selection.py::test_e2_lifting_uniform_safety_is_law_independent_and_cancellation_free
tests/test_odlrq_selection.py::test_e2_p1_p2_cocycles_match_products_weighted_norms_and_limited_derivations
tests/test_odlrq_selection.py::test_e2_return_memory_split_stationary_semantics_and_finite_sum_are_exact
tests/test_odlrq_selection.py::test_e2_orientation_basis_weight_split_and_transport_mutations_fail_closed
tests/test_odlrq_selection.py::test_e2_caps_horizon_work_and_preallocation_bombs_fail_before_authority
tests/test_odlrq_selection.py::test_e2_fixed_candidate_universe_prevents_prefilter_omission_and_accepts_boundary
tests/test_odlrq_selection.py::test_e2_gate_is_coverage_complete_nonempty_reachable_and_top_ranking_binding
tests/test_odlrq_selection.py::test_e2_strict_roundtrip_invalidation_tier_firewall_and_nominal_fallback
```

The exact launch mechanism is this inline Windows PowerShell 5.1 control block,
run from the clean detached candidate worktree.  `$nodes` is the exact ordered
array printed above; it is not caller supplied.

```powershell
foreach ($name in @(
  "PYTEST_ADDOPTS", "PYTEST_PLUGINS", "PYTHONPATH", "PYTHONHOME",
  "PYTHONNOUSERSITE", "PYTHONUSERBASE", "PYTHONWARNINGS"
)) {
  Remove-Item -LiteralPath ("Env:" + $name) -ErrorAction SilentlyContinue
}
$env:PYTEST_DISABLE_PLUGIN_AUTOLOAD = "1"
$env:PYTHONDONTWRITEBYTECODE = "1"
$env:PYTHONHASHSEED = "0"
$nodes = @(
  "tests/test_odlrq_selection.py::test_e2_square_parent_coordinate_identification_rederives_complete_typed_basis",
  "tests/test_odlrq_selection.py::test_e2_restriction_replays_full_parent_terminal_zeros_and_restricted_weights",
  "tests/test_odlrq_selection.py::test_e2_lifting_uniform_safety_is_law_independent_and_cancellation_free",
  "tests/test_odlrq_selection.py::test_e2_p1_p2_cocycles_match_products_weighted_norms_and_limited_derivations",
  "tests/test_odlrq_selection.py::test_e2_return_memory_split_stationary_semantics_and_finite_sum_are_exact",
  "tests/test_odlrq_selection.py::test_e2_orientation_basis_weight_split_and_transport_mutations_fail_closed",
  "tests/test_odlrq_selection.py::test_e2_caps_horizon_work_and_preallocation_bombs_fail_before_authority",
  "tests/test_odlrq_selection.py::test_e2_fixed_candidate_universe_prevents_prefilter_omission_and_accepts_boundary",
  "tests/test_odlrq_selection.py::test_e2_gate_is_coverage_complete_nonempty_reachable_and_top_ranking_binding",
  "tests/test_odlrq_selection.py::test_e2_strict_roundtrip_invalidation_tier_firewall_and_nominal_fallback"
)
$arguments = @("-m", "pytest", "-q", "-p", "no:cacheprovider") + $nodes
$process = Start-Process -FilePath "C:\Python313\python.exe" `
  -ArgumentList $arguments -WorkingDirectory $PWD.Path -NoNewWindow -PassThru
if (-not $process.WaitForExit(180000)) {
  try { $process.Kill() } finally { $process.WaitForExit() }
  exit 124
}
exit $process.ExitCode
```

The wall starts when `Start-Process` returns the process object.  The child
inherits the operator console; no output file, redirection, parser, or secondary
capture channel is used.  Exit `124` is the frozen timeout disposition.  Any
other nonzero exit is a failed qualification.  Qualification requires exact
console summary `10 passed`, exit zero, and zero failure, error, skip, xfail,
deselection, warning-based substitution, or extra collection.  The invocation
is consumed when `Start-Process` is called, including launch failure, pass,
test failure, timeout, import/collection error, console/tool capture error, or
operator interruption.

There is deliberately no committed or external custom runner, P/Invoke, Job
Object, stdout/stderr parser or redirector, latch, marker, receipt,
archive/materialization layer, private runtime copy, EvidenceLedger, CAS,
publisher, terminal-observation schema, guard change, or workflow change.  The
fixtures are fixed, source-embedded, synthetic values and read no protected
task or benchmark.  A control failure is recorded honestly; it does not license
replacement control infrastructure.

Immediately before consumption, and in this order, the operator checks once:

1. the pushed authority ref equals the final authority commit and its exact
   authority CI gate above has completed;
2. remote `codex/uprime-odlrq-plan` still equals accepted E1 `6fb35aa`, remote
   candidate and qualification-closeout refs are absent, and the semantic
   commit is not directly advertised by a remote head or tag;
3. a newly created detached worktree has HEAD `6998f2f`, tree `3512b6b`, sole
   parent E1, exact subject, exactly the four path/mode/blob rows in section 3,
   and a clean index/worktree;
4. the current directory is that resolved worktree root; and
5. the interpreter path, version, SHA-256, pytest version, NumPy version, and
   sanitized environment absence/presence values match this section.

Any mismatch stops before `Start-Process` and consumes no Q0 invocation, but it
also licenses no correction to authority, source, test, workflow, or runner.

This deliberately lightweight qualification is not a tamper-resistant runtime
attestation.  Pytest and NumPy are version/path checked in the mutable user-site,
not committed as a full byte manifest; local output is retained by the Codex
terminal transcript rather than a second receipt; the 180-second wall begins
after `Start-Process` returns; and `Kill()` targets the direct child.  The frozen
E2 sources/tests perform no child-process or network action.  A launch hang,
dependency drift, missing transcript, or unexpected descendant is a Q0 control
failure and stop, never grounds for adding the machinery deliberately excluded
above.

## 5. Publication and acceptance order

Only an exact local `10 passed` result licenses the one-time creation of remote
candidate ref `codex/uprime-e2-qualification-candidate` at `6998f2f`.
Its natural push CI is the only candidate Actions attempt; workflow dispatch
and Actions rerun are forbidden.  Candidate qualification requires:

```text
event        push
head SHA     6998f2f9ec430881df50e6790ef9a8f13b1b7857
summary      2610 passed, 8 skipped, 161 deselected
failed nodes 0
```

Only that green candidate CI licenses a fast-forward of
`codex/uprime-odlrq-plan` from E1 `6fb35aa` to E2 `6998f2f`.  The accepted-ref
push must produce a distinct natural CI at the identical semantic SHA, with the
same exact counts and no failed node.  The accepted ref is not moved on local
or candidate failure, and it is never rewound.

After the accepted CI is observed, or after the first failure at any earlier
stage, exactly one concise document-only qualification closeout may be added as
the sole child of the final authority commit:

```text
path    docs/experiments/uprime_odlrq_e2_qualification_only_closeout_2026-07-16.md
mode    100644
subject uprime: close E2 qualification-only re-entry
ref     codex/uprime-e2-qualification-closeout
push    one non-force creation; never amended, repointed, merged, or force-pushed
```

It records the local outcome and any candidate/accepted run and job IDs that
actually exist.  Its one natural push CI must have the same exact control-only
red/count/node/zero-E2 shape as the authority CI.  Exact observation closes as
`U24_E2_QUALIFICATION_CLOSEOUT_CI_OBSERVED`; mismatch closes as
`U24_E2_QUALIFICATION_CLOSEOUT_CI_MISMATCH`.  There is no Actions rerun or
repository correction.  No per-test amendment, result triplet, terminal bridge
record, or bridge schema extension is created.

## 6. Budgets and stopping rules

```text
authority amendment commits       1
authority correction commits      0
authority Actions attempts         1 natural push; rerun 0
semantic/source/test commits       0
Windows qualification invocations 1
candidate ref creations            1 only after local green
candidate Actions reruns            0
accepted fast-forwards             1 only after candidate green
accepted Actions reruns             0
terminal closeout documents        at most 1
terminal closeout Actions attempts at most 1 natural push; rerun 0
custom runner/control repairs      0
```

Any local import, collection, test, timeout, resource, environment, Git, or
capture failure closes qualification without repair or a second invocation.
Any candidate or accepted CI red, unexpected count, unexpected node, wrong
event/ref/SHA, or extra run closes without source correction, workflow repair,
or Actions rerun.  Failure is engineering qualification evidence only; it is
not evidence against the E2 mathematics.

Success accepts only the exact declared finite synthetic E2 endpoint: exact
restriction, lifting-uniform finite safety, P1/P2 finite cocycle, finite return
memory, complete three-candidate universe, and a nonempty ranking-binding
support gate on the fixed fixtures.  It does not establish an all-germ,
infinite-horizon, production-Lean, or solve-rate result.

## 7. Historical CI clarification required for future readers

**Dated clarification, 2026-07-16:** the immutable U05 result commit
`cc91a4181a9f87ec10f11727ed787eb7149f955a` has red run `29166670576`, job
`86580832840`, because the guard omitted shallow-history handling.  Audit
authority `628d3cc64af2531da3a527bad335d9e5158294a7` adjudicated that cause.
The exact scientific candidate `3bb3408afc50a08307cff2c9b1906a299739dfb5`
had green run `29166073728`, job `86579287017`.  The red result badge is a
control/guard design defect and must not be read as a scientific U05 failure.
This clarification does not change the independent KP3-D4 scientific failure
or the present E2 qualification rules.

## 8. Deferred work and nonclaims

This amendment licenses no ME0/MaxEnt construction, S0 predictive or positive
similarity, I0 integration, response-monoid extension, U'1.5-L0/ODLRQ learner,
protected K1--K4 read, Amendment A completion, reserved/valid task access,
canonical RPC rerun, GPU, SSH, LLM proposer, or LLM distillation.  LLM remains
outside the loop until the exact theoretical generator and its downstream
finite pipeline have been accepted independently.

If E2 is accepted, the next work begins under a new authority based on accepted
E2.  It may then address ME0 MaxEnt strictly as nominal model selection inside
the fixed support; it may not treat MaxEnt as the source of safety.

## 9. Adversarial implementation-plan disposition

Three read-only audits were run before this draft:

1. roadmap audit confirmed KP1--KP3 and WP4-E/H/W/WP6-T are already complete
   and E2 is the actual next dependency;
2. instrument audit confirmed the depth-four history apparatus already exists
   and repeating it is not the next scientific action; and
3. governance audit rejected another construction/runner authority and required
   this execution-only, zero-repair, direct-pytest design.

The phase is approved only with the budgets and separation above.  In
particular, an apparent need for a custom runner is a stop condition, not a
license to resume control-plane construction.
