# U-prime / ODLRQ post-E2 ME0--S0--I0 continuation amendment

Date: 2026-07-17 (Asia/Tokyo)

Status: **FROZEN WHEN THIS DOCUMENT-ONLY SIDECAR COMMIT IS COMMITTED AND
PUSHED ON ITS NEW AUTHORITY REF.**  It is never merged into the accepted
scientific first-parent line.

This amendment resumes the public-synthetic upper-stack construction after
accepted E2.  It registers the complete ME0--S0--I0 implementation roadmap but
licenses source and qualification for **ME0 only**.  S0 and I0 remain uniquely
specified mathematical work packages and require one later post-ME0 phase
authority with exact node/resource/publication topology; they cannot begin
under this document.  This is not a new review of the already-adjudicated
theory, a protected endpoint, or a license to revive the former control-plane
runner lineage.

## 1. Accepted base and scientific interpretation

The sole base is accepted E2:

```text
commit  7a8b28872439dd61d40174c2500c5990790002be
subject uprime: repair E2 fixture namespace and serialization
ref     codex/uprime-odlrq-plan
```

Its single licensed Windows-CPU qualification passed exactly `10 passed` in
271.36 seconds.  Candidate CI run `29548427172` / job `87785632743` and the
distinct accepted CI run `29548712422` / job `87786490144` were green with
exactly `2610 passed, 8 skipped, 161 deselected`.  The accepted endpoint binds
the exact quotient-coordinate restriction, P/Q split, lifting-uniform finite
safety, P1/P2 finite cocycles, finite return-memory sum, complete declared
three-candidate universe, and ranking-binding certified support token.

### Deterministic accepted-token extraction

On 2026-07-17, before this freeze, the accepted fixture was constructed once
with the frozen Windows runtime.  `token.certified_support_token_sha256` was
machine-equal to SHA-256 of `canonical_contract_bytes(token.to_dict())`.  The
extraction binds:

```text
accepted commit  7a8b28872439dd61d40174c2500c5990790002be
accepted tree    d54ed9fab52da4929843fabdeb3c1e1920994f6a
wire bytes       2185
wire/token SHA   D01170427E717D543D941740881C937EF5B535E357D67EEFDBF62773AFD6E660
support IDs      ["c0","c2"]
```

The exact canonical JSON wire is:

```json
{"abstained_candidate_ids":[],"candidate_universe_manifest_sha256":"327DDC3DBD63C049A1B16B570B81F5DDECCE1B8C3C7F83734609C83B12501D9A","comparator":"exact_rational_less_equal_v1","coverage":{"denominator":"3","numerator":"2","schema_version":"lean-rgc-odlrq-exact-rational-v1"},"decision_rows":[{"authority_bundle_sha256":"6B29E9EC02EBBC4C36A1AD4B9A485E54954D05345E4DCB8BE2135A3D61B6BF2C","bound":{"denominator":"1","numerator":"1","schema_version":"lean-rgc-odlrq-exact-rational-v1"},"candidate_id":"c0","decision":"ACCEPT","reason":"BOUND_LE_THRESHOLD","threshold":{"denominator":"1","numerator":"2","schema_version":"lean-rgc-odlrq-exact-rational-v1"}},{"authority_bundle_sha256":"AF2491D9640434CFD7798465887A28138F2879C7C31608C204F12A4E53E6630E","bound":{"denominator":"1","numerator":"3","schema_version":"lean-rgc-odlrq-exact-rational-v1"},"candidate_id":"c1","decision":"REJECT","reason":"BOUND_GT_THRESHOLD","threshold":{"denominator":"1","numerator":"2","schema_version":"lean-rgc-odlrq-exact-rational-v1"}},{"authority_bundle_sha256":"FF2C37D9A917559DC759512162EA82E72A5CED7F558EF624A577876F152F97C4","bound":{"denominator":"1","numerator":"2","schema_version":"lean-rgc-odlrq-exact-rational-v1"},"candidate_id":"c2","decision":"ACCEPT","reason":"BOUND_LE_THRESHOLD","threshold":{"denominator":"1","numerator":"2","schema_version":"lean-rgc-odlrq-exact-rational-v1"}}],"denominator":3,"endpoint_id":"u24_e2_declared_square_endpoint_v1","gated_ranking":["c2","c0"],"invalidation_sha256":"B83E0F2608123C8EAC140332DA5F00E5B2F293229EC44D6087628B93DBF591EB","numerator":2,"p1_cocycle_sha256":"6C87E7EE21B8BC0D78D024AB14C2D5F247D541531A90D6291732D284C7FFEF11","p2_cocycle_sha256":"BEE7B16BC7FF8AF926CDF8F5502F21B2708A9C4C280F57AC846889B2C50A065D","ranking_changed":true,"rejected_candidate_ids":["c1"],"return_memory_bound_sha256":"95C2BEDA13B1085E46183038F857B753AE0DC531685BC3996EB1E5F5AFAD4A46","schema_version":"odlrq.e2.certified-support-token.v1","support_candidate_ids":["c0","c2"],"threshold":{"denominator":"1","numerator":"2","schema_version":"lean-rgc-odlrq-exact-rational-v1"},"ungated_ranking":["c1","c2","c0"],"verification_disposition":"E2_BINDING_SUPPORT_CERTIFIED"}
```

Q0 and the first Q1 control attempt failed for implementation/control reasons
before a scientific read.  They are not evidence against the quotient,
envelope, MaxEnt, or similarity program.  The successful Q1 closeout is the
document-only sidecar commit `bad3da20dd0bb6393a5029bf20131ad8e0c29c9d`.

### 2026-07-17 red-CI clarification for future readers

The immutable U05 result commit
`cc91a4181a9f87ec10f11727ed787eb7149f955a` has red CI run
`29166670576`, job `86580832840`.  The dated audit identified a
shallow-history omission in the guard, not a scientific failure.  The exact
candidate commit `3bb3408afc50a08307cff2c9b1906a299739dfb5` passed green
candidate CI run `29166073728`, job `86579287017`.  A future reader must not
interpret the red result badge as a failed kill probe.  Likewise, the red Q1
authority/closeout badges are intentional sidecar-topology control results and
do not reverse the green E2 candidate and accepted results above.

## 2. Why continuation is licensed now

The completed dependency chain is:

```text
U'0.5 / U'1.5 KP1--KP3
  -> exact admission and partition refinement
  -> tier firewall, bounded Hankel predictor, componentwise window
  -> generation-time canonical history and exact depth-four core
  -> E0 exact quotient-coordinate generator
  -> E1 exact declared finite-fiber envelope
  -> E2 restriction, finite cocycle/memory, and binding support gate
```

The registered dispositions remain:

```text
KP1 U05_KP1_SCALE_READY
KP2 U05_KP2_EVENTUAL_WINDOW
KP3 U05_KP3_PLATEAU_AT_D3
```

WP4-E, WP4-H, WP4-W, and WP6-T are implemented development assets.  The
fresh-family KP3 depth-four scientific value was not observed because its
launcher failed before evaluation; its canonical-history and exact Hankel
cores are nevertheless already implemented.  That consumed endpoint is not
reopened here, and it is not a dependency of ME0.

Independent read-only implementation-plan audits were conducted by the
`post_e2_plan_audit`, `post_e2_code_audit`, and `post_e2_theory_map` agents in
the Codex task that produced this document.  Their pre-freeze findings were
incorporated into this revision: sidecar topology, exact support-row binding,
Pinsker/KL admissibility, nontrivial fixtures, immutable predecessor
specification, and explicit publication paths.  They agreed that (a) no
mathematical stop condition exists,
(b) repeating the theory review or kill probes would add no licensed evidence,
(c) ME0 is the first missing code layer, and (d) locality learning remains
downstream of I0.  The audits attacked the dependency order, code coverage,
endpoint governance, runner feasibility, and mathematics-to-code map rather
than re-reviewing the 2026-07-10 theoretical verdict.

## 3. Frozen order and branch topology

The registered semantic order is:

```text
accepted E2 -> ME0 -> S0 -> I0 -> closeout
```

The authority ref is
`codex/uprime-post-e2-upper-stack-authority`.  Its document-only commit is a
single-parent child of accepted E2 and is a Q1-style sidecar: it is not an
ancestor of ME0, S0, I0, or the accepted ref.  Because the historical topology
validator intentionally rejects an unregistered document row, its natural CI
must have exactly the sole failure
`test_u24_b0_anchor_contiguous_budget_and_terminal_topology`, zero new-stage
module/node hits, and no other failure.  This known authority-side control red
is expected to summarize as `2609 passed, 1 failed, 8 skipped, 161 deselected`
and is not a stage result.  An unexpected authority-CI shape blocks source work.

```text
parent       7a8b28872439dd61d40174c2500c5990790002be
changed path docs/experiments/uprime_odlrq_post_e2_me0_s0_i0_continuation_amendment_2026-07-17.md
path mode    100644
subject      docs: freeze post-E2 ME0-S0-I0 continuation
remote ref   codex/uprime-post-e2-upper-stack-authority
```

That ref is created once by non-force push and is never moved, deleted,
rewritten, or rerun.  Before ME0 source exists, the remote ref, sole parent,
document path/mode/blob, commit subject, workflow identity, expected sole
authority-CI failure, run attempt, and exact count are machine-resolved and
checked.  The resulting authority commit, document blob, natural CI run ID, and
job ID are copied into both `maxent.py` and `test_odlrq_maxent.py` as:

```text
ME0_AUTHORITY_COMMIT_SHA
ME0_AUTHORITY_PARENT_SHA
ME0_AUTHORITY_DOCUMENT_PATH
ME0_AUTHORITY_DOCUMENT_BLOB_SHA
ME0_AUTHORITY_CI_RUN_ID
ME0_AUTHORITY_CI_JOB_ID
```

The ME0 candidate preflight fresh-fetches the authority ref and checks all six
values before local qualification and again before candidate publication.  The
authority commit/blob are included in the declared support reference, problem
wire, and result wire.  A source tree lacking that binding or naming a different
sidecar is outside this authority.

The ME0 candidate is instead a separate single-parent direct child of the
current accepted ref and changes exactly the ME0 allowlist.  Source work
begins only after the predecessor's distinct accepted CI is green and the
sidecar authority-CI shape above is verified.  Publication requires the
current stage-local suite and natural candidate CI to be green.  The accepted
ref then advances by fast-forward only to the byte-identical candidate; the
later S0 authority may freeze only after a distinct natural accepted-ref ME0 CI
is green.  No
merge, cherry-pick, force-push, CI rerun, or protected-data look substitutes
for this order.

The primary candidate ref is `codex/uprime-me0-candidate`; replacement attempts,
if needed, append `-a2` or `-a3`.  A candidate ref is created once and never
moved, deleted, forced, or rerun.

This bundle deliberately replaces per-unit freeze/amendment/execution document
triples.  Deterministic engineering tests may be rerun during development.
Endpoint semantics, tier boundaries, and the allowed path set are frozen here;
ordinary implementation and CI defects are repaired inside the ME0 allowlist
before publication.  A red published candidate remains on
its immutable ref; at most two fresh replacement attempts may be rebuilt as
new direct children of the unchanged accepted predecessor, so the finally
accepted history still contains exactly one commit for the stage and does not
exceed the already-consumed historical correction counter.  Three red
publication attempts stop the stage for a rational-remedy report to Fable; user
escalation still requires a program-level theoretical refutation or exhaustion
of rational repairs.

The normative ME0, S0, and I0 contracts are inherited from sections 11--13 of
`docs/experiments/uprime_odlrq_u2_u4_development_construction_bundle_amendment_2026-07-13.md`
at Git blob `bd3ef021dff5cb5e3a28c1d2a79b0379e5615835`.  This amendment changes
execution and publication topology, not those equations, fixture rows, caps,
tier separation, artifact schemas, or success dispositions.  Explicit
bindings and fallbacks below close implementation ambiguities without weakening
that immutable contract.

## 4. Execution model and the retired runner

All work in this amendment is local Windows CPU plus natural repository CI.
There is no one-look scientific endpoint, attempt marker, custom capture
wrapper, evidence ledger, synthetic CAS, or new runner.

The historical `tools/run_uprime_u2_u4_development_tests.ps1` names ME0/S0/I0,
but its ME0 lane includes predecessor suites under a 180-second wall.  The
accepted E2 suite alone required 271.36 seconds in the licensed run and 316.09
seconds in cold development.  The old lane is therefore mechanically
infeasible and remains bound to a closed topology authority.  It must not be
silently reused or recalibrated.

ME0 instead uses:

1. direct stage-local pytest during development;
2. one registered cold stage-local Windows qualification after exact
   collect-only equality; and
3. the unchanged natural CI workflow as the whole-repository regression gate.

The ME0 qualification runtime is `C:\Python313\python.exe` with executable
SHA-256 `D932E5E2F324D57F392E8FD063DCF6D0185BE8A664C57C6D24E7762ED02C28CA`,
Python 3.13.7, pytest 9.0.3, and NumPy 2.3.3.  It sets
`PYTEST_DISABLE_PLUGIN_AUTOLOAD=1`, `PYTHONDONTWRITEBYTECODE=1`,
`PYTHONHASHSEED=0`, and sets `OPENBLAS_NUM_THREADS`, `OMP_NUM_THREADS`,
`MKL_NUM_THREADS`, `VECLIB_MAXIMUM_THREADS`, and `NUMEXPR_NUM_THREADS` all to
`1`.  Float64 outputs must pass on both that Windows runtime and natural Linux
CI; the canonical wire and tolerances, rather than bit-identical LAPACK
intermediates, are the portability contract.

The canonical `uprime.me0.windows-runtime.v1` manifest uses ASCII JSON with
sorted keys and separators `(',', ':')`.  It is 1231 bytes with SHA-256
`F20A2C1A6556EAAC5371C7438A5F588A3F7E5A76282E2F500614B2E43FF6C05A`.
Besides the versions, Python digest, thread map, and exact OpenBLAS label
`OpenBLAS 0.3.30 USE64BITINT DYNAMIC_ARCH NO_AFFINITY Haswell MAX_THREADS=24`
above, its ordered binary table is:

```json
{"binary_files":[["numpy/__init__.py",26478,"2A5AFA55E398F495F0BA78604982075873997AFB43D5C4701A8B968EFFDA6A69"],["numpy/_core/_multiarray_umath.cp313-win_amd64.pyd",4529664,"3F8A35487C05180F4F6B6168165935C99FAE6F14264A443FB665C5CB07FA2725"],["numpy/linalg/_umath_linalg.cp313-win_amd64.pyd",111104,"5B7F5ECB0970EE956E36608D4F1ED405C0B688691E99C05F5F314C2F053DBA10"],["numpy/linalg/lapack_lite.cp313-win_amd64.pyd",18432,"52FD5D7FB455463349E3DCB99A723AE331256FCBE2197C8F0192090CBD576A2D"],["numpy.libs/libscipy_openblas64_-860d95b1c38e637ce4509f5fa24fbf2a.dll",20403712,"860D95B1C38E637CE4509F5FA24FBF2A98BA8696F9F3D28BF184BEE74AD9A325"],["numpy.libs/msvcp140-a4c2229bdc2a2a630acdc095b4d86008.dll",575056,"A4C2229BDC2A2A630ACDC095B4D86008E5C3E3BC7773174354F3DA4F5BEB9CDE"]],"blas_label":"OpenBLAS 0.3.30 USE64BITINT DYNAMIC_ARCH NO_AFFINITY Haswell MAX_THREADS=24","numpy_version":"2.3.3","pytest_version":"9.0.3","python_executable_sha256":"D932E5E2F324D57F392E8FD063DCF6D0185BE8A664C57C6D24E7762ED02C28CA","python_version":"3.13.7","schema_version":"uprime.me0.windows-runtime.v1","thread_environment":{"MKL_NUM_THREADS":"1","NUMEXPR_NUM_THREADS":"1","OMP_NUM_THREADS":"1","OPENBLAS_NUM_THREADS":"1","VECLIB_MAXIMUM_THREADS":"1"}}
```

| relative binary | bytes | SHA-256 |
|---|---:|---|
| `numpy/__init__.py` | 26478 | `2A5AFA55E398F495F0BA78604982075873997AFB43D5C4701A8B968EFFDA6A69` |
| `numpy/_core/_multiarray_umath.cp313-win_amd64.pyd` | 4529664 | `3F8A35487C05180F4F6B6168165935C99FAE6F14264A443FB665C5CB07FA2725` |
| `numpy/linalg/_umath_linalg.cp313-win_amd64.pyd` | 111104 | `5B7F5ECB0970EE956E36608D4F1ED405C0B688691E99C05F5F314C2F053DBA10` |
| `numpy/linalg/lapack_lite.cp313-win_amd64.pyd` | 18432 | `52FD5D7FB455463349E3DCB99A723AE331256FCBE2197C8F0192090CBD576A2D` |
| `numpy.libs/libscipy_openblas64_-860d95b1c38e637ce4509f5fa24fbf2a.dll` | 20403712 | `860D95B1C38E637CE4509F5FA24FBF2A98BA8696F9F3D28BF184BEE74AD9A325` |
| `numpy.libs/msvcp140-a4c2229bdc2a2a630acdc095b4d86008.dll` | 575056 | `A4C2229BDC2A2A630ACDC095B4D86008E5C3E3BC7773174354F3DA4F5BEB9CDE` |

ME0 results bind that runtime-manifest digest.  The preflight checks each local
length/hash before qualification; natural Linux CI checks portable equations,
tolerances, and strict wire behavior but does not claim the Windows artifact
identity.

The exact ME0 child command is:

```text
C:\Python313\python.exe -m pytest -q -p no:cacheprovider tests/test_odlrq_maxent.py
```

Collection must equal the nine node IDs frozen in section 6.  Their UTF-8
LF-joined list without trailing newline has SHA-256
`0B29C6C862B586A88BECCB49F97688CCE4998D949F49DBDC0FC3A3829D7E0D3F`.
Qualification
requires exactly `9 passed`, with zero failure, error, skip, xfail, warning
substitution, deselection, or extra collection.  The direct stage suite must
finish within 180 seconds; exceeding that registered bound blocks publication
and triggers algorithmic diagnosis rather than runner construction.  Natural
candidate CI must then report exactly `2619 passed, 8 skipped, 161 deselected`;
the distinct accepted CI must report the identical count.

The registered local invocation is an inline, unpersisted PowerShell supervisor:
it verifies the runtime manifest and exact collect-only node list, starts only
the child command above with inherited console, calls `WaitForExit(180000)`,
kills once on timeout, waits at most 10 additional seconds for cleanup, and
returns the exact child code.  No stdout/stderr capture, receipt, marker, retry,
Job Object, P/Invoke, or runner file is used.  Thus 180 seconds is an enforced
hard wall.  The historical runner's 2-GiB/output caps are superseded for ME0 by
the strict support/dimension/subset/bit/allocation caps and the nine-node quiet
command; S0/I0 inherit no such supersession and must register enforceable caps
later.

A need for a new control framework is a design defect to remove, not a reason
to build another ledger layer.  The single later S0/I0 phase authority must
freeze its commands, exact node lists, counts, and enforceable bounds before S0
source exists; no per-unit document triplets are authorized.

## 5. Mathematics-to-code map

| Mathematical object | Code owner | Verification owner | Tier |
|---|---|---|---|
| exact quotient coordinates | `quotient_generator.py` | existing E0/E1 suites | exact |
| worst-case finite-fiber envelope | `envelope.py` | existing E1 suite | hard synthetic |
| finite cocycle, memory, support gate | `certificates.py`, `selection.py` | existing E2 suite | hard synthetic |
| MaxEnt lifting on fixed support | `maxent.py` | `test_odlrq_maxent.py` | nominal |
| predictive/global positive representations | `similarity.py` | `test_odlrq_similarity.py` | predictive vs hard typed |
| telescoping pipeline bound and artifacts | `evals/uprime_u2_u4_development.py` plus `certificates.py` | I0 extension tests | typed integration |
| synthetic locality CEGAR | later `local_region.py`, `locality_cegar.py` | separate post-I0 authority | exact splits; statistical no-counterexample |

This preserves the theoretical construction order imported from the fiber
closure work: generation-time normal form, exact quotient, positive worst-case
majorant, then MaxEnt model selection and finite-level global similarity.
Signed cancellation may simplify exact quotient coordinates but is never the
source of the positive safety bound.

Design provenance is the UTF-8 theory in
`fiber_closure_fixed_point_integrated_jp.tex`, the S'1m postmortem/upper-stack
text supplied as `pasted-text.txt`, and the completed structural techniques in
`fiber_closure_generator_part2_jp.tex` plus its experiment archive.  The
persisted 2026-07-10 adversarial review is
`docs/external/uprime_odlrq_adversarial_review_2026-07-10/`.  NS-side numerical
values are not Lean-side evidence: only completed structural methods such as
generation-time normal form, exact-before-positive ordering, ghost retention,
and separator proposals are transported.

## 6. ME0 -- MaxEnt inside immutable E2 support

ME0 changes exactly:

```text
lean_rgc/odlrq/maxent.py
lean_rgc/odlrq/__init__.py
tests/test_odlrq_maxent.py
tests/tier_manifest.json
```

The frozen public surface is:

```text
MaxEntStatus
DeclaredE2SupportReference
MaxEntProblem
MomentGeometry
OrbitReferenceLaw
OperatorSpanResidual
MaxEntResult
bind_e2_support
make_declared_e2_support_reference
classify_exact_moment_geometry
solve_finite_fiber_maxent
verify_maxent_result
```

`maxent.py` may import the exact token type for binding but exposes no envelope,
safety-certificate, selection-gate, or support-token constructor/converter.
Every result has tier `NOMINAL_MODEL_SELECTION_ONLY`.

The public binding endpoint requires exact type
`type(token) is CertifiedSupportToken`, freshly calls its strict `to_dict()`,
hashes the canonical wire, and takes support IDs only from that wire.  Reference
mass, statistics, orbit sizes, row loads, nominal-operator rows, and exact-rule
columns are candidate-keyed tables and must form a duplicate-free,
omission-free bijection in exactly that support order.  Their common row-table
digest is repeated in every result.  A digest accompanied by separately
caller-ordered rows is not a live E2 binding.

The accepted E2 commit did not persist its token wire, so section 1 records the
single pre-freeze deterministic extraction.  ME0 unit tests embed and rehash
that exact wire as a frozen `DeclaredE2SupportReference` and do not rebuild
E0--E2.
Such a digest reference is explicitly `NOMINAL_MODEL_SELECTION_ONLY`, is not a
`CertifiedSupportToken`, and is rejected by every envelope/safety API.  A live
token object is rebound once in I0.

The result repeats exactly the same support binding.  ME0 may change nominal
probability and nominal order only within the E2 support; it cannot add or
remove an atom, admit rejected `c1`, create an abstention decision, or construct
a hard-support capability.

For strictly positive reference mass `q_i`, orbit size `o_i`, sufficient
statistic `phi_i`, target `m`, and dual parameter `lambda`, the implemented law
is:

```text
q_orbit,i proportional to q_i / o_i
p_i(lambda) = q_orbit,i exp(lambda dot phi_i - log Z(lambda))
KL(p || q_orbit) = sum_i p_i log(p_i / q_orbit,i)
```

All `q`, `phi`, target, `eta`, row loads, nominal-operator values, and exact-rule
columns are reduced `ExactRational`; orbit sizes are positive exact integers.
The exact-rule matrix `G`, its row/column order, shape, and digest are part of
the immutable problem wire and may not be selected after observing `k`.

Preflight caps are support `<=32`, declared statistic dimension `<=4`, orbit
size `<=32`, input rational numerator/denominator bit length `<=256`, exact
intermediate numerator/denominator bit length `<=4096`, vectorized operator
dimension `<=32`, exact-rule column count `<=32`, operator-matrix cells
`<=1024`, and exact subset work
`sum_(k=1)^(d+1) binom(n,k) <= 242824`.  Exact reduced-rational arithmetic
classifies, in this order:

```text
malformed input -> hard StrictContractError
affine rank below declared dimension -> SINGULAR_STATISTICS
target outside convex hull -> OUTSIDE_HULL
target on a supporting hull face -> BOUNDARY_NO_FINITE_PARAMETER
strict hull interior -> numeric solver
```

Membership uses lazy Caratheodory subset enumeration through `d+1`; it never
materializes the subset table.  After full affine-rank and membership checks,
exact supporting-facet enumeration distinguishes boundary from interior.  This
must classify the center of a square as interior even though it may lie on a
chosen triangulation diagonal.  Every rational operation checks the
intermediate bit cap; the separately frozen lazy subset count is the exact
enumeration-work cap, so no second unspecified operation counter exists.
All row/matrix shapes and cell counts are validated before tuple/matrix copying
or NumPy import.

Only an interior target reaches NumPy.  The sole solver is float64 stable
log-sum-exp with zero initial lambda, damped Newton/pseudoinverse,
`rcond=1e-12`, at most 128 iterations, at most 32 halving backtracks, moment
infinity norm `<=1e-10`, simplex error `<=1e-12`, dual residual `<=1e-10`, and
no nonfinite intermediate.  Exhaustion is `NUMERIC_FAILURE`; it is never
relabelled as boundary, outside, or safe.  A private test-only zero-iteration
configuration may exercise that branch but is not accepted by the public API.
Float conversion overflow, an underflowed zero probability, or a nonfinite
log-partition/Hessian/residual is `NUMERIC_FAILURE`.

Every finite float is serialized by Python `format(x, ".17g")`; its wire uses a
lowercase exponent and must satisfy decode--reencode identity and identical
binary64 bits.  Raw JSON floats,
NaN, infinity, negative zero, locale-dependent text, unknown fields, and
noncanonical rational wires fail closed.

The checker records support, orbit normalization, simplex, moment, dual, KL,
operator-span, and Pinsker diagnostic residuals.  Operator-span uses the same
fixed pseudoinverse.  For a nonnegative row load `r` and exact KL radius `eta`,
the diagnostic is:

```text
E_p[r] <= E_q_orbit[r] + (max(r) - min(r)) * sqrt(eta / 2)
```

The checker reports the right-hand side and separately records
`kl_within_radius = (KL(p || q_orbit) <= eta)`.  If false, the Pinsker value is
unavailable and the fixed nominal fallback is the ascending canonical ID inside
the already-certified support; no outside atom is considered.  Numeric failure
and every non-`INTERIOR_SOLVED` disposition use the same within-support
fallback while preserving the exact geometry/numeric status; a fallback is
never relabelled as a solved law.  The solver status still records the
correct geometry and is not rewritten.  This is a nominal robust
diagnostic only.  It cannot produce, widen, replace, or invalidate an E1/E2
safety token.

The fixed primary fixture uses support `[c0,c2]`, statistics `[0,2]`, reference
`[1/2,1/2]`, orbit sizes `[1,1]`, interior target `1`, boundary target `0`,
outside target `3`, singular statistics `[[1],[1]]`, KL radius `1/20`, and
operator-span tolerance `1e-10`.  The nontrivial orbit/Newton fixture uses
orbit sizes `[1,2]`, target `4/5`, row load `[1,3]`, exact rule column
`G=[[1],[2]]`, and nominal operator `k=[2,4]`; it must take a nonzero-lambda
solution, rederive orbit reference `[2/3,1/3]`, have zero operator-span residual
within `1e-10`, and satisfy the registered KL radius.  A second interior target
`1/5` must remain `INTERIOR_SOLVED` while failing `kl_within_radius` and choosing
fallback `c0`.  A four-corner square with target center guards against the
triangulation-boundary bug in the pure exact classifier; it is not substituted
for the accepted two-atom E2 support.  All five dispositions must be reachable:

```text
INTERIOR_SOLVED
BOUNDARY_NO_FINITE_PARAMETER
OUTSIDE_HULL
SINGULAR_STATISTICS
NUMERIC_FAILURE
```

The exact ME0 nodes, in order, are:

```text
tests/test_odlrq_maxent.py::test_me0_frozen_token_wire_rehashes_and_live_binding_rejects_counterfeit_rows
tests/test_odlrq_maxent.py::test_me0_exact_geometry_classifies_singular_outside_boundary_and_square_center
tests/test_odlrq_maxent.py::test_me0_primary_fixture_solves_and_preserves_the_fixed_support
tests/test_odlrq_maxent.py::test_me0_nontrivial_orbit_fixture_requires_nonzero_lambda_and_meets_residuals
tests/test_odlrq_maxent.py::test_me0_kl_radius_is_separate_from_geometry_and_uses_within_support_fallback
tests/test_odlrq_maxent.py::test_me0_numeric_failure_is_never_relabelled_or_promoted
tests/test_odlrq_maxent.py::test_me0_operator_span_and_pinsker_outputs_are_nominal_diagnostics_only
tests/test_odlrq_maxent.py::test_me0_strict_wire_roundtrip_rejects_raw_float_nonfinite_negative_zero_and_mutation
tests/test_odlrq_maxent.py::test_me0_caps_fire_before_subset_materialization_or_numpy_and_no_safety_surface_exists
```

ME0 success is `CPU_SYNTHETIC_MAXENT_CORE_VERIFIED`.

## 7. S0 -- typed finite-level global similarity

This section is a frozen roadmap/specification inheritance, not an S0 source or
execution license.  A later authority may license S0 only after accepted ME0.
Its intended semantic allowlist remains:

```text
lean_rgc/odlrq/similarity.py
lean_rgc/odlrq/__init__.py
tests/test_odlrq_similarity.py
tests/tier_manifest.json
```

It implements `LocalTower`, `GlobalMeasure`, disjoint final
`PredictiveDistance` and `PositiveDistance` types, declared-synthetic `L_+`,
and exact R/N/G morphisms.  Predictive discrepancy includes recorded numeric
and cross-covariance remainders; positive safety requires hard target residual,
coverage, and a checked declared-synthetic `L_+` token.  Learned `L_+`, omitted
residual, or zero sampled violations cannot construct a safety majorant.

The fixed schedule is
`(1,1,1)->(2,1,1)->(2,2,1)->(2,2,2)` and the finite Cauchy majorant is the exact
sequence `[3/4,1/2,1/4,0]`.  This is a finite registered remainder certificate,
not an infinite-cutoff theorem.  S0 success is
`CPU_SYNTHETIC_TYPED_SIMILARITY_CORE_VERIFIED`.

All S0 metric equations, zero-mass rule, fine-to-coarse column-stochastic map
direction, composition order, hard weights, coverage/target-residual fixture
rows, digest bindings, and finite-remainder inequalities are normative exactly
as inherited from the blob in section 3.  The hard
`DeclaredSyntheticLPlusToken` binds E1/E2 hard authorities and the complete S0
universe independently of ME0.  ME0 appears only in predictive/nominal
provenance and cannot construct or alter the positive channel.

## 8. I0 -- typed integration and canonical artifacts

This section is a frozen roadmap/specification inheritance, not an I0 source,
execution, or artifact-emission license.  A later authority may license I0 only
after accepted S0.  Its intended semantic allowlist remains:

```text
lean_rgc/odlrq/certificates.py
lean_rgc/odlrq/__init__.py
lean_rgc/evals/uprime_u2_u4_development.py
tests/test_uprime_u2_u4_development.py
tests/tier_manifest.json
```

It constructs `TypedPipelineBound`, keeps the hard residual separate from the
nominal MaxEnt addendum, abstains on missing coverage, and emits canonical
artifacts for `envelope_core`, `maxent_fixture`, `local_tower`,
`global_measure`, `level_transport`, `similarity_certificate`, and
`integrated_certificate`.  A nominal term cannot enter the hard safety sum.
I0 success is `CPU_SYNTHETIC_U2_U4_CANDIDATE_CONSTRUCTED`.

The normative telescoping equation is:

```text
e_final <= (product_i L_i) e_0
           + sum_i (product_(j>i) L_j) epsilon_i.
```

The exact inherited pass fixture must independently recompute hard bound
`3/4`, nominal addendum `1/10`, and total report `17/20`; the fail fixture has
hard bound `15/16`, and coverage `3/4` is the abstain fixture.  The ordered
candidate manifest binds E0 generator, E1 envelope, both E2 cocycle/support
authorities, ME0 result, S0 certificate, exact stage labels, and runtime
identity.  All I0 coefficients, artifact schemas/common fields/payload key
order, and verification rules are normative exactly as inherited from the blob
in section 3.

The later S0/I0 phase authority must freeze, before S0 source exists, exact S0
and I0 node lists, commands, collect-only counts, no-skip rules, enforceable
walls/memory/output caps, candidate/accepted CI counts, unique post-E2
success/failure refs and closeout paths, and one exact artifact-emission command.
It must use the I0-allowed identity-test extension to register those unique
terminal path sets rather than reusing any immutable R6 pathname.  Emitted
`source_commit` binds the already accepted I0 semantic commit and never a
self-referential artifact commit.  No I0 source, output directory, placeholder
artifact, closeout, or emission command is authorized here.

## 9. Locality learner, Amendment A, and LLM boundary

After successful I0 closeout, a separate U'1.5-L0 amendment may implement the
synthetic locality CEGAR.  Its frozen design direction is:

- retain ghost actions even when direct response is zero;
- use before-only locality, mvar-sharing separator, cycle-rank, and treewidth
  features;
- allow exact Lean counterexamples to split regions only;
- treat no-counterexample and merge claims as statistical;
- optimize conditional-covariance reduction per query cost; and
- record coverage, abstention, baseline, seed, and deterministic tie-breaks.

Low-rank Schur/separator corrections from the fiber-closure work are proposal
structure, not imported numerical evidence.  Native Lean-oracle L1 begins only
after a fresh public-synthetic official-transport qualification.  A filled
Amendment A and protected K-series remain downstream of both I0 and transport.

LLM proposing, model weights, `llm_local.json`, `pilot_tasks.json`, reserved
tasks, and knowledge distillation remain outside this amendment.  LLM work is
considered only after the exact theoretical generator, finite upper pipeline,
and theory-driven locality proposer work independently.

## 10. Stop and repair rules

The current stage stops publication, but not rational engineering repair, on:

- a support-token digest or candidate-ID mismatch;
- MaxEnt support mutation or construction of a safety token;
- noninterior exact classification entering the numeric solver;
- numeric failure promoted to a hard/robust pass;
- a false KL/Pinsker admissibility claim or outside-support fallback;
- any S0/I0 source, artifact, closeout, or emission attempt under this ME0-only
  license;
- protected/reserved data access, GPU, SSH, LLM, or native Lean execution; or
- an allowed-path violation.

Implementation errors, Windows/CI defects, and test failures are diagnosed and
repaired within the frozen semantics and active allowlist.  They are not
mathematical failures.  User direction is requested only if the upper program
is theoretically refuted or no rational repair remains.
