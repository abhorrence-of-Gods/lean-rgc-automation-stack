# U-prime / ODLRQ U2--U4 development reconstruction amendment

Date: 2026-07-13 (Asia/Tokyo)

Status: **FROZEN ONLY AFTER THIS DOCUMENT IS COMMITTED, PUSHED, AND GREEN ON
BOTH ITS CANDIDATE REF AND THE ACCEPTED BRANCH**

Sole parent: accepted development-construction failure closeout
`214dec3adb7841452fea19f7ae668d8e0f7520a1`.

That parent passed candidate CI run `29223789103`, job `86733872378`, and the
exact same commit passed accepted CI run `29223926295`, job `86734237210`.

## 1. Narrow purpose and verdict

This is new prior authority for reconstructing only the failed B0 engineering
gate and then returning immediately to the already frozen E0--I0 upper-stack
construction. It does not repair, rewrite, or continue the rejected build
lineage, and it does not reopen the 2026-07-10 review of the upper mathematics.

The rejected correction commit
`7ca946ef6c23cf0855cd3942eecbe20663f70e21` passed the registered Windows B0
runner locally but failed candidate CI run `29223348434`, job `86732622987`.
The fixed editable install created six untracked `lean_rgc.egg-info` files;
the full-status identity gate classified them as E0 dirt. The accepted failure
closeout records this as `U24_B0_IDENTITY_OR_RUNNER_BLOCKED`, not as a
scientific endpoint failure and not as a shallow-history incident.

The reconstruction admits those six names only as visible and untrusted CI
setup residue. After `guard.install_guard`, registered semantic collection and
execution may not read them. Every other mathematical object, fixture, formula,
cap, endpoint, and nonclaim remains frozen by the original authority.

## 2. Inherited authority and exact supersession

The original authority is commit
`14234e209229931c00615d4b171620ec6d1bbbf5`, document
`docs/experiments/uprime_odlrq_u2_u4_development_construction_bundle_amendment_2026-07-13.md`,
blob `bd3ef021dff5cb5e3a28c1d2a79b0379e5615835`.

Inherited byte-for-byte are:

- the mathematical definitions, weighted quotient algebra, fixtures,
  constructor grammar, schemas, thresholds, tolerances, and endpoints;
- the exact/certified/observed/nominal tier firewall and evidence scopes;
- the fixed E0, E1, E2, ME0, S0, I0, EMIT, and CLOSEOUT requirements;
- the Python, NumPy, OpenBLAS, thread, runtime-hash, wall, memory, output, and
  receipt identities;
- the union source allowlist except for the exact B0R workflow addition below,
  protected-read boundary, absent artifact root, artifact schemas, and one-shot
  EMIT/CLOSEOUT order;
- the prohibitions on K1--K4 reads, native Lean/RPC, official transport, SSH,
  remote CPU, GPU, LLM, deployment, and protected execution;
- section 14's stop/nonclaim set and section 15's post-success order; and
- the dated paragraph distinguishing U05 result-CI run `29166670576` from the
  green scientific candidate run `29166073728`.

Only the following clauses are superseded:

1. section 4's parent, ref names, and build topology;
2. section 5's union source allowlist, solely to add
   `.github/workflows/ci.yml` for B0R static/tracked identity; E0--I0 path sets
   remain unchanged. This addition closes the hosted pre-guard pytest
   entry-point route exposed by admitting untrusted `entry_points.txt`;
3. section 6's denial-root cardinality, solely to add
   `lean_rgc.egg-info/`;
4. section 7's B0 gate, replaced by B0R below; and
5. sections 13--14's closeout ref names, filenames, and failure parents, only
   as explicitly restated here.

An unlisted conflict resolves in favor of the original authority. E0--I0 are
not respecified, relaxed, or re-reviewed by this amendment.

## 3. Refs, ancestry, and commit budget

The only registered refs are:

```text
A1       codex/uprime-u2-u4-development-r1-a0
build    codex/uprime-u2-u4-development-r1-build
closeout codex/uprime-u2-u4-development-r1-closeout
failure  codex/uprime-u2-u4-development-r1-failure-closeout
accepted codex/uprime-odlrq-plan
```

A1 is exactly one commit adding only this document to sole parent
`214dec3...`. No B0R source may exist until A1 has green candidate CI, has
been fast-forwarded exactly to `accepted`, and has distinct green accepted CI.

The build ref then starts from accepted A1. Its fixed order is:

```text
B0R -> E0 -> E1 -> E2 -> ME0 -> S0 -> I0
```

There are at most seven named, single-parent stage commits and at most one
bundle-wide correction commit, hence at most eight build commits. History is
append-only: no amend, reset, rebase, merge, replacement ref, deletion,
force-push, or same-SHA CI rerun as a repair. A correction is available only
after a committed stage fails its registered local lane or after a red pushed
candidate or accepted CI; uncommitted local engineering is not a correction.
It may touch only the failed stage's frozen path set and may
not change a fixture, formula, endpoint, tolerance, wall, cap, setup-residue
set, denial root, or allowlist. A correction failure, second correction,
unexpected setup path, or ninth build commit closes R1.

Before each stage commit is created, deterministic dirty-worktree iteration
and one blocker-only adversarial review must be green on the intended bytes.
The append-only stage commit is then created. Before push, its registered clean
Windows lane and every stage-specific residue probe must be green on that
commit. A committed local-lane failure consumes the sole correction child if
one is still available; otherwise it closes R1. Only a locally qualified commit
is pushed. The remaining order is candidate green CI, exact accepted
fast-forward, and distinct green accepted CI. No next-stage source exists
before both runs are green. An accepted CI failure is never repaired by
rewinding the accepted ref.

There is one consolidated R1 success closeout or one R1 failure closeout. No
stage-specific amendment/result triplet, ledger, CAS, publisher, recovery
framework, second/additional runner or runner family beyond the single
registered reconstructed runner, or recursive repair phase is permitted.

## 4. Rejected B0 as untrusted engineering provenance

The rejected build commits are:

```text
3e6331a6b1bbcca3ca3acfea02daeb0c8de62406  initial B0
7ca946ef6c23cf0855cd3942eecbe20663f70e21  sole correction
```

The corrected blobs are registered only as code-review provenance:

```text
identity  e88b0d15f1fa8121bfea34956e3032facb5541ed
guard     158971b0dfba8201704bd1098eb66bf24c3c593c
runner    d6ac6ed053db8ed88a7b4468cc53f8fa96c5f142
manifest  47997a9bd67f7ac2cf0cd9c7f654d8f069f925d8
```

They may be consulted and selectively re-expressed, but neither rejected
commit may be an R1 ancestor and no merge or cherry-pick is allowed. Their
local receipts, reviews, forged-pyc attack, and red CI are negative engineering
witnesses only. They provide no R1 qualification or scientific evidence.

## 5. B0R path set and anti-fractal cap

B0R changes exactly five paths:

```text
.github/workflows/ci.yml
tests/test_uprime_u2_u4_development.py
tests/uprime_u24_guard.py
tools/run_uprime_u2_u4_development_tests.ps1
tests/tier_manifest.json
```

It may not change `.gitignore`, `pyproject.toml`, a scientific module, fixture,
input, artifact, or any other path. The workflow change only sets
`PYTEST_DISABLE_PLUGIN_AUTOLOAD: "1"` for the existing Test step; no action,
command, trigger, permission, runner, or other environment value may change.
The manifest change only registers `test_uprime_u2_u4_development.py` as
`unit`.

The frozen `UNION_SOURCE_PATHS`, expected-union assertion, runner tracked-path
list, and static identity include `.github/workflows/ci.yml`; its bytes are
frozen after the exact Test-step environment addition. `STAGE_ALLOWLISTS`
licenses that path only in B0R. Later stages may neither change nor remove it.

B0R must finish within two calendar days and eight engineer-hours. Beyond the
mechanical re-expression of the four rejected blobs, residue-specific work is
limited to one frozen exact tuple, one NUL-safe raw-status validator in the
existing control builder, one pure status-partition helper, one added denial
root, one Test-step environment line, one canonical control-schema version
extension described below, and targeted assertions inside the existing five
direct B0 test functions. B0R may not add a test module, metadata parser,
digest ledger, generic generated-file registry, cleanup subsystem, pip wrapper,
publisher, or second schema field/family. The functional inventory in this
paragraph and the eight-hour wall are the anti-fractal cap; no subjective line
classification is used.

The full inherited phase remains bounded by seven calendar days and 40
engineer-hours from A1 acceptance.

## 6. Exact visible setup-residue contract

The frozen residue root is `lean_rgc.egg-info/`. The frozen exact path set is:

```text
lean_rgc.egg-info/PKG-INFO
lean_rgc.egg-info/SOURCES.txt
lean_rgc.egg-info/dependency_links.txt
lean_rgc.egg-info/entry_points.txt
lean_rgc.egg-info/requires.txt
lean_rgc.egg-info/top_level.txt
```

The control attestation is extended once from
`u24-control-plane-attestation-v1` to
`u24-control-plane-attestation-v2`. The sole added canonical field is
`ci_setup_paths`; no other field or schema family is authorized. The existing
`status_paths` field continues to retain every path; no entry is hidden,
ignored, deleted, or rewritten.

The control builder alone invokes and strictly parses:

```text
git status --porcelain=v1 -z --untracked-files=all --no-renames
```

Its one shared validator receives the NUL-delimited status records and the
repository root. It rejects malformed/duplicate records, invalid XY codes,
absolute or noncanonical paths, and every residue state not admitted below. It
may populate `ci_setup_paths` only after checking exact `??` status codes and
the filesystem structure below. The strict decoder requires `ci_setup_paths`
to be either an empty sorted list or the complete sorted exact-six list and to
be a subset of `status_paths`. A single exported pure helper takes both
canonical sequences and derives governed stage dirt. It never reparses
metadata content.

Residue is admitted in exactly two states: absent, or the complete six-path
set. In the complete state every Git status code is exactly `??`, the root is a
real directory below the repository, every child is a regular
non-symlink/non-reparse file, and direct root enumeration contains exactly the
six frozen filenames and no directory or seventh/nested entry. A partial set,
root-only entry, seventh entry, nested entry, case variant, duplicate,
non-string, unsorted sequence, staged/tracked/renamed entry, symlink, junction,
reparse point, directory substitution, or path escape blocks.

Only the builder-validated `ci_setup_paths` subset is subtracted for
stage-topology purposes:

```text
stage_dirty = governed_status_paths(status_paths, ci_setup_paths)
```

Any arbitrary path outside the root remains in `stage_dirty`; any non-exact
path under the root blocks before topology. The runner's pre-semantic control
check and EMIT/CLOSEOUT status comparison must call the same helper rather than
duplicating an exception.

The root becomes the seventeenth frozen semantic denial row in both the Python
guard and the runner's byte-identical compact denylist. After guard
installation, all modes deny read, write, stat, enumeration, and move below it.
It is not an EMIT exception. Existing static import rules continue to reject
`importlib` use in bundle source. Both the registered semantic child and the
hosted CI Test step set `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1` before pytest
collection, closing the pre-test `entry_points.txt` plugin-autoload route
without parsing or cleaning the residue.

B0R control and post-guard semantic code never parse, hash, or trust the six
contents, and never use them in a FrameId, operator identity, candidate
identity, receipt, fixture, or artifact. B0R does not claim that pip created
them or that their contents are authentic, reproducible, supply-chain safe, or
unable to affect an arbitrary pre-guard hostile bootstrap. Their sole authority
is to exist as visible setup residue under the exact structural contract above.

## 7. B0R required attacks and acceptance

The existing direct-five B0 surface is retained. Assertions inside it must
cover:

1. absent residue returns unchanged stage dirt;
2. exact-six residue is visible in raw status and subtracts to empty;
3. exact six plus legal B0 dirt preserves that B0 dirt;
4. five-of-six, a seventh/nested/root-only/case-variant entry, or a
   staged/tracked/renamed exact name blocks;
5. exact six plus arbitrary outside dirt preserves that dirt and fails the
   existing stage allowlist;
6. symlink/reparse/directory substitution blocks;
7. `open`, `Path.read_*`, `stat`, `listdir`, `scandir`, and glob access below
   the residue root emits `U24_RESOURCE_OR_SCOPE_BLOCKED`;
8. raw control retains the exact residue in both `status_paths` and the sole
   v2 `ci_setup_paths` field while every runner status decision uses the pure
   governed helper;
9. the Python/PowerShell denial copies, cardinality 17, runner hash, identity
   core hash, exact CI Test-step environment, CRLF/LF normalization, and
   bare-carriage-return rejection remain exact; and
10. forged repository `.pyc`, shallow history, wrong head, merge parent,
    accepted-ref drift, future-lane invocation, skip, xfail, deselection,
    wrapper, function-set manipulation, and correction-path expansion remain
    blocked as inherited.

Uncommitted qualification requires the inherited 29-test local suite and the
blocker-only review. After the B0R commit is created, a disposable clean
worktree at that exact commit must first prove the residue root absent. Only in
that residue-absent worktree, it runs
`python -m pytest --collect-only -q` once with plugin autoload unset and once
with `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1`; after normalizing path separators, the
ordered lines matching `^tests/.*::` must be byte-identical, with no duplicate
node ID, and both commands must exit zero. The autoload-ON leg is forbidden
whenever `lean_rgc.egg-info/` exists.
Pre-push qualification then requires the exact registered Windows command

```powershell
& .\tools\run_uprime_u2_u4_development_tests.ps1 -Lane B0
```

on clean and exact-six worktrees, a negative partial/seventh residue probe, and
an exact-six `entry_points.txt` sentinel probe showing that registered
post-guard collection neither imports nor executes the sentinel. The sentinel
probe runs with plugin autoload disabled only.
B0R retains the 60-second wall, 20-second three-times qualification threshold,
2-GiB peak Job memory cap, 64-MiB output cap, fixed runtime identity, and
success disposition `CPU_U24_IDENTITY_AND_RUNNER_GATE_VERIFIED`.

Because B0R adds no test function and repairs the sole failed existing B0
function, each pushed B0R Actions test step must report exactly `2581 passed`,
`8 skipped`, `161 deselected`, zero failed, and zero xfailed tests. Any smaller,
larger, or differently censored surface blocks rather than being explained by
plugin disabling.

The pushed candidate and accepted Actions runs are the only integration tests
of the actual `python -m pip install -e ".[test]"` residue and the frozen
hosted-CI plugin-autoload boundary. B0R is not accepted until both distinct
runs are green on the identical commit/tree.

## 8. Return to the mathematical construction

After B0R acceptance, the next commit is E0. Its path set, fixtures, exact
formula `Delta_a e_C = e_(tau_a(C)) - e_C`, terminal totalization, source seal,
strict deserialization, `B*A` work count, caps, attacks, runner lane, and
success disposition are exactly section 8 of the original authority. E1, E2,
ME0, S0, and I0 then follow original sections 9--12 without alteration. B0R is
not licensed to grow after acceptance and no further identity/runner work may
displace E0.

## 9. Closeout and failure topology

The R1 closeout filenames are:

```text
docs/experiments/uprime_odlrq_u2_u4_development_reconstruction_closeout_2026-07-13.md
docs/experiments/uprime_odlrq_u2_u4_development_reconstruction_failure_closeout_2026-07-13.md
```

Success otherwise inherits section 13 exactly. A failure closeout adds only
the R1 failure document and imports no absent artifact. At every closure point,
including A1, committed-local, candidate, accepted-CI, correction, semantic
stage, and success-closeout failure, the R1 failure closeout is a single-parent child
of the current `codex/uprime-odlrq-plan` tip at the moment closure is declared.
Before an accepted failure this is the last accepted predecessor; after an
accepted red run it is that immutable red accepted commit. This universal rule
also applies if a correction child of an accepted-red commit later fails, so no
accepted ref is ever rewound. A failure-closeout CI failure stops without
repair or recursion.

The original closed terminal labels are reused without relabelling. No failed
stage is rerun, weakened, or continued under this authority.

## 10. Deferred hardening and nonclaims

This amendment deliberately does not add residue content hashes, metadata
parsing, pip/setuptools provenance, hardlink provenance, build-output
relocation, a general ignored-file policy, bootstrap-wide hostile-repository
proof beyond the frozen pytest-autoload boundary, compromised-runner proof, or
concurrent-filesystem-race proof. Its NUL-safe parser is limited to the one
registered Git status command and is not a general ambient-file framework. It
claims only clean-checkout, single-process registered execution with the exact
fixed ASCII residue names and the post-guard semantic read-denial boundary
above. Any broader claim requires separate prior authority; none may be added
during R1.

Failure is not evidence that the upper mathematical program is impossible.
No outcome here claims protected K1--K4 performance, same-family D4 rank,
production Lean locality, complete all-germ quotient, production hard
envelope, MaxEnt safety, global Lean similarity, learner improvement, solve
rate, deployment, remote/GPU work, or LLM benefit.
