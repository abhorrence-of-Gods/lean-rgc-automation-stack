# U-prime / ODLRQ U'2--U'4 synthetic development construction bundle

Date: 2026-07-13 (Asia/Tokyo)

Status: **FROZEN ONLY AFTER THIS DOCUMENT IS COMMITTED, PUSHED, AND GREEN ON
BOTH ITS CANDIDATE REF AND THE ACCEPTED BRANCH**

Sole parent: accepted official-transport recovery failure closeout
`78c12549eb188610977842edbc38e2723d469ba4`.

This is the single development-construction bundle anticipated by section 14
of the accepted upper-stack plan.  It does not re-review the upper mathematical
theory adjudicated on 2026-07-10.  It translates the surviving implementation
obligations into one bounded Windows-CPU build from exact quotient coordinates
through a synthetic integrated certificate.

## 1. Authority and historical adjudication

The following accepted blobs are inputs, not editable members of this phase:

| authority | Git blob |
|---|---|
| upper-stack implementation plan and U05 amendment | `2b2355f49aef149c1a7b5493951fa10e4a254235` |
| lane-isolated M4--M6 closeout | `5061650434a1fb5dde29ff1b6dd1e48fdb117182` |
| KP3-D4 closeout | `7205667906d2b7a7baaff8a41d68b813839cbecb` |
| official-transport recovery failure closeout | `d94d9890f0c0bdf90f1e47bf49f779c5fbb31928` |

The M4--M6 closeout supplies only its declared synthetic-development
capabilities: exact finite partition, the exact/certified/observed/nominal tier
firewall, bounded rational prediction machinery, and a componentwise
diagnostic.  The D4 and transport closeouts are terminal engineering failures
before a scientific rank/fiber endpoint.  They supply no D4 rank, compression,
hard envelope, or Lean-domain-completeness evidence.

The D4 phase executed and closed, but its endpoint failed before a feasibility
decision; D4 rank/compression therefore remains undecided.  Acting on the later
user-directed route before any new protected read, this amendment narrowly
supersedes lane-closeout lines 164--178 only to allow synthetic-development
U'2--U'4 construction without treating D4 as positive evidence.  It does not
supersede the ban on a new protected one-shot.  D4 section 9 still requires
qualified official transport and a new frozen family before such a look.  A
final transport qualification remains mandatory before Amendment A or any
protected execution, but it is not on this synthetic bundle's critical path.

### 2026-07-13 red-CI clarification for future readers

The immutable U05 result-publication commit
`cc91a4181a9f87ec10f11727ed787eb7149f955a` has red Actions run `29166670576`
(job `86580832840`).  Dated audits found an identity-guard shallow-history
design omission, not a probe, prerequisite, artifact-integrity, or scientific
failure.  The exact scientific candidate
`3bb3408afc50a08307cff2c9b1906a299739dfb5` had green run `29166073728` (job
`86579287017`).  The red result badge must not be read as a failed scientific
execution.  This bundle neither rewrites nor reruns that consumed endpoint.

The current sole parent passed candidate CI run `29218636726`, job
`86719328351`, and accepted CI run `29218769747`, job `86719697408`.

## 2. External structural provenance and no-copy boundary

The already registered external inputs are:

| file | SHA-256 | admitted use |
|---|---|---|
| `fiber_closure_generator_part2_jp.tex` | `FED9A116D04E56D41FC6A306B1C85CA85399B92375322ED45EED546E4FE126A7` | completed finite-support structural techniques and warnings |
| `fiber_closure_generator_part2_experiments.zip` | `1FA90B92C0998FAEC941F658BA295B34790568924022A068EFC2CA03C69C95E2` | static exact-data-structure patterns only |

Only completed structural techniques transfer: generation-time normal form,
exact normalization and quotient before a positive majorant, exact-rational
structure separated from later floating nominal fitting, and finite orbit/cache
patterns.  No Navier--Stokes coefficient, norm, numerical result, trajectory,
or completion label is Lean evidence.  This phase does not read either external
file at runtime.

## 3. Narrow construction question

The registered question is:

> On already verified synthetic exact finite quotient coordinates, can one
> construct a source-bound positive finite-horizon worst-case majorant, then a
> nominal MaxEnt law inside its fixed support, and finally typed predictive and
> positive global similarities with explicit finite-approximation transports,
> without using observed/nominal data to manufacture a hard certificate?

The noninterchangeable build order is:

```text
verified exact finite partition and tier firewall
  -> exact quotient-coordinate transfer layer
  -> positive finite-fiber envelope
  -> finite-horizon cocycle / pressure / reachable lexicographic gate
  -> MaxEnt nominal law constrained inside the certified support
  -> predictive and positive global similarities with R/N/G transports
  -> one synthetic typed telescoping certificate and CPU calibration
```

The hard path is complete without a learner.  Lean-oracle locality CEGAR is a
later nominal proposal/query bundle and may never create an exact merge or hard
upper bound.  LLM proposal or distillation remains last, after the
theory-driven generator and upper pipeline independently work.

## 4. Frozen refs, topology, and anti-fractal budget

The fixed refs are:

```text
A0       codex/uprime-u2-u4-development-a0
build    codex/uprime-u2-u4-development-build
closeout codex/uprime-u2-u4-development-closeout
failure  codex/uprime-u2-u4-development-failure-closeout
accepted codex/uprime-odlrq-plan
```

A0 is exactly one commit adding only this document.  No source, test, runner,
fixture, or artifact may be created before A0 candidate and accepted CI are
green.

The build ref starts from accepted A0 and contains at most eight pushed,
single-parent, non-merge commits: the named `B0/E0/E1/E2/ME0/S0/I0` commits and
at most one correction commit for the whole bundle.  History is append-only:
no amend, rebase, reset, replacement ref, deletion, or force-push.  A red
candidate remains in history and may have at most one correction child; two
consecutive red candidates, a second correction, or an eighth build commit
without I0 completion closes the bundle.  Every
milestone must pass candidate CI and accepted-branch CI before the next
milestone starts.

For every milestone, the green build candidate SHA is fast-forwarded exactly,
without merge or content change, to `codex/uprime-odlrq-plan`.  Candidate and
accepted CI are distinct Actions runs on the identical commit/tree.  Any
accepted-ref drift, non-fast-forward, tree mismatch, or CI head mismatch stops
the bundle before the next source edit.

The sole correction, if used, may change only the failed milestone's own path
set listed below.  It preserves every fixture byte/grammar, schema, formula,
endpoint, cap, wall, predecessor blob, and earlier stage object.  Cross-stage
repair, allowlist growth, weakened assertion, or changed numeric tolerance
stops the bundle.

There is one consolidated success closeout commit or one failure closeout
commit.  The failure ref adds only
`docs/experiments/uprime_odlrq_u2_u4_development_failure_closeout_2026-07-13.md`
and never imports an artifact.  No component-level amendment,
result triplet, new ledger, CAS, publisher, recovery framework, or recursive
repair phase is permitted.  Local uncommitted iteration is ordinary
deterministic engineering because no protected result is read.

The total budget is seven calendar days or forty active engineer-hours, whichever
comes first.  All semantic construction uses local Windows CPU.  Each frozen
lane runner owns its child process, qualifies only when its already fixed wall
is at least three times its first standalone cold measurement, and is capped at
2 GiB peak job memory and 64 MiB combined stdout/stderr.  The final hard walls,
fixed now and never recalibrated, are 60 seconds for B0, 60 seconds for E0,
120 seconds for E1, 180 seconds for E2, 180 seconds for ME0, 300 seconds for S0,
900 seconds each for I0 and EMIT, and 60 seconds for CLOSEOUT validation.  Cold measurements audit whether each wall is at least
three times observed runtime; a failed margin stops the lane and never raises a
cap.  There is one process, one worker, no parallelism, and at most 2 GiB of
bundle artifacts.

The numeric runtime is Windows x86-64 Python `3.13.7` at
`C:\Python313\python.exe`, SHA-256
`D932E5E2F324D57F392E8FD063DCF6D0185BE8A664C57C6D24E7762ED02C28CA`,
NumPy `2.3.3`, with `_multiarray_umath.cp313-win_amd64.pyd` SHA-256
`3F8A35487C05180F4F6B6168165935C99FAE6F14264A443FB665C5CB07FA2725` at
`C:\Users\yusei\AppData\Roaming\Python\Python313\site-packages\numpy\_core\_multiarray_umath.cp313-win_amd64.pyd`.
The loaded linear-algebra binary is
`C:\Users\yusei\AppData\Roaming\Python\Python313\site-packages\numpy\linalg\_umath_linalg.cp313-win_amd64.pyd`,
SHA-256 `5B7F5ECB0970EE956E36608D4F1ED405C0B688691E99C05F5F314C2F053DBA10`.
BLAS/LAPACK is
`C:\Users\yusei\AppData\Roaming\Python\Python313\site-packages\numpy.libs\libscipy_openblas64_-860d95b1c38e637ce4509f5fa24fbf2a.dll`,
SHA-256 `860D95B1C38E637CE4509F5FA24FBF2A98BA8696F9F3D28BF184BEE74AD9A325`,
scipy-openblas `0.3.30`, `USE64BITINT`, Haswell dynamic architecture.  The
strict canonical runtime manifest containing these paths/hashes, Python/NumPy
versions, BLAS label, and thread variables has SHA-256
`D6E6DEBCE5C150AE31BA0D04EAF6E59FD2D79FDC4C0D5272264574665C0242F4`.
`runtime_identity_sha256` is mandatory in the candidate manifest and every
artifact.  The runner sets `OMP_NUM_THREADS=1`, `OPENBLAS_NUM_THREADS=1`,
`MKL_NUM_THREADS=1`, `NUMEXPR_NUM_THREADS=1`, and rejects identity/config drift.

The single runner has the exact lane interface below; no other selector or test
command qualifies a lane:

```powershell
& .\tools\run_uprime_u2_u4_development_tests.ps1 -Lane B0
& .\tools\run_uprime_u2_u4_development_tests.ps1 -Lane E0
& .\tools\run_uprime_u2_u4_development_tests.ps1 -Lane E1
& .\tools\run_uprime_u2_u4_development_tests.ps1 -Lane E2
& .\tools\run_uprime_u2_u4_development_tests.ps1 -Lane ME0
& .\tools\run_uprime_u2_u4_development_tests.ps1 -Lane S0
& .\tools\run_uprime_u2_u4_development_tests.ps1 -Lane I0
& .\tools\run_uprime_u2_u4_development_tests.ps1 -Lane EMIT
& .\tools\run_uprime_u2_u4_development_tests.ps1 -Lane CLOSEOUT
```

Each lane selects only its named tier-manifest node plus all predecessor nodes,
rejects skips/xfails, and prints one canonical JSON receipt with exactly
`schema_version`, `lane`, `commit`, `tests_passed`, `wall_ticks`,
`clock_frequency`, `peak_job_memory_bytes`, `output_bytes`, `artifact_sha256`,
and `disposition`; `artifact_sha256` is null except for EMIT's exact sorted
path-to-digest map.  The runner performs no artifact publication except EMIT.
EMIT activates the denial guard before importing the emitter, owns the Python
child in the same Windows Job Object, and enforces the I0 900-second, 2 GiB, and
64 MiB caps.  `CLOSEOUT` exists only after the
one official emission and validates tracked artifact bytes without importing
any later-stage source or regenerating a payload.

## 5. Frozen union allowlist

Build commits may change only a subset of:

```text
lean_rgc/odlrq/contracts.py
lean_rgc/odlrq/quotient_generator.py
lean_rgc/odlrq/envelope.py
lean_rgc/odlrq/maxent.py
lean_rgc/odlrq/similarity.py
lean_rgc/odlrq/selection.py
lean_rgc/odlrq/certificates.py
lean_rgc/odlrq/__init__.py
lean_rgc/evals/uprime_u2_u4_development.py
tests/test_odlrq_quotient_generator.py
tests/test_odlrq_envelope.py
tests/test_odlrq_maxent.py
tests/test_odlrq_similarity.py
tests/test_odlrq_selection.py
tests/test_uprime_u2_u4_development.py
tests/uprime_u24_guard.py
tools/run_uprime_u2_u4_development_tests.ps1
tests/tier_manifest.json
```

The accepted base blobs for the pre-existing substrate are:

```text
contracts.py                    eca7d55bc7c2a7a08fbdc75c3b589f1972cd258f
quotient_generator.py           e8d95082d2e47f0829d960321cc1d62bc686d7ac
odlrq/__init__.py               866df99205f5073335ea7304b0cc160ab657e8f5
test_odlrq_quotient_generator.py d014b0c9555ac5932827d86b6b92420849ed4973
tests/tier_manifest.json        88eb765c7c0eb484825e39b956860621db71bc84
```

The first build commit adds
`tests/test_uprime_u2_u4_development.py` as an identity test that freezes A0,
its parent, these
base blobs, the union allowlist, and the contiguous commit budget.  Historical
quotient modules outside `lean_rgc/odlrq/` remain quarantine/baseline code and
are not extended into the hard path.

## 6. Input, exposure, and capability boundary

Permitted inputs are inline hand-authored finite fixtures, exhaustive finite
families generated deterministically inside tests, and already-contaminated
public development examples that are re-declared in this amendment before use.
No task-level performance endpoint is read.

The complete lane fixture tables/grammars are frozen in sections 8--13.  No
fixture generator may inspect the repository, clock, process environment,
random device, network, or prior artifact.  Its canonical bytes and SHA-256 are
reported by the tests and frozen in the final closeout; changing a displayed
fixture table, order, or grammar requires stopping this bundle, not selecting a
new fixture.

The following are forbidden for the whole bundle:

```text
docs/experiments/inputs/
docs/experiments/artifacts/
runs/
lean_rgc/native_lean/RGCKernelRPC.lean
tools/uprime_official_transport_v2_smoke.py
tools/run_uprime_official_transport_v2_smoke.ps1
tools/run_uprime_official_transport_v2_tests.ps1
tests/test_uprime_official_transport_v2_smoke.py
C:\Users\yusei\Desktop\lean_rgc_automation_stack_v47_goal_state_dynamics\llm_local.json
C:\Users\yusei\Desktop\lean_rgc_automation_stack_v47_goal_state_dynamics\pilot_tasks.json
C:\Users\yusei\Desktop\lean_rgc_automation_stack_v47_goal_state_dynamics\fake_lean_smoke.py
C:\Users\yusei\Desktop\lean_rgc_automation_stack_v47_goal_state_dynamics\smoke_tasks_local.jsonl
C:\Users\yusei\.codex\quarantine\lean-rgc\uprime-deferred-2026-07-12\
C:\Users\yusei\Desktop\lean_rgc_automation_stack_v47_goal_state_dynamics_transport_v2_live\
C:\Users\yusei\Desktop\lean_rgc_automation_stack_v47_goal_state_dynamics_transport_v2_i1\
C:\Users\yusei\Desktop\lean_rgc_automation_stack_v47_goal_state_dynamics_transport_v2_recovery_live\
```

No native Lean subprocess, canonical RPC, network, SSH, remote CPU, GPU/CUDA,
model server, LLM proposer, LLM distillation, or deployment action is allowed.
The root scratch names `llm_local.json`, `pilot_tasks.json`,
`fake_lean_smoke.py`, and `smoke_tasks_local.jsonl` are outside the repository
and outside every input glob.  No code in this bundle may glob the repository
root.

For B0--I0, every listed prefix/path is read-denied.  EMIT has one exception:
it may create/write, but never pre-read, only
`docs/experiments/artifacts/uprime_u2_u4_development_20260713/`, which must not
exist at entry.  CLOSEOUT may read only the exact seven output files named in
section 13 and may not write them.  No other artifact/input/runs exception
exists.

Every public E0--I0 witness, token, operator, result, measure, morphism,
certificate, verification report, and artifact wire has mandatory exact fields
`evidence_scope=synthetic_development` and
`domain_scope=declared_finite_totalized_snapshot_only`.  Strict deserialization
rechecks both; omission, alteration, unknown fields, or attempted scope
promotion is a required kill test in every lane.

B0 installs an autouse denial guard for every semantic test and the runner
activates it before importing bundle code.  It rejects `open`, `os.open`,
`pathlib` reads, directory enumeration, subprocess, socket, and dynamic import
when the resolved path/capability matches any forbidden row above.  B0 also
statically rejects those literals/imports in the union source allowlist, except
for one exact denylist constant in `tests/uprime_u24_guard.py` and its identical
copy in the runner; their canonical bytes must match.  A
denial is `U24_RESOURCE_OR_SCOPE_BLOCKED`; it is not a skip.  CI checkout and
read-only Git/Actions queries performed outside the semantic child remain the
only network/control-plane exception.

## 7. B0 -- identity, runner, and tier gate

B0 is the first build commit and changes only:

```text
tests/test_uprime_u2_u4_development.py
tests/uprime_u24_guard.py
tools/run_uprime_u2_u4_development_tests.ps1
tests/tier_manifest.json
```

It freezes the A0 blob and parent, every accepted authority/base blob, the union
allowlist, ref topology, named commit sequence, correction quota, lane commands,
walls, and forbidden paths.  It tests that no pre-A0 source exists and that all
four evidence tiers rederive their public fields from retained authority rather
than trusting wire booleans.  It builds no generator or mathematical object.
B0 candidate and accepted CI must be green before E0 source is created.
Success: `CPU_U24_IDENTITY_AND_RUNNER_GATE_VERIFIED`.

## 8. E0 -- exact quotient-coordinate generator

E0 changes only `lean_rgc/odlrq/quotient_generator.py`,
`lean_rgc/odlrq/__init__.py`, and
`tests/test_odlrq_quotient_generator.py`.  It adds the schemas
`odlrq_exact_quotient_coordinate_term_v1`,
`odlrq_exact_quotient_transfer_row_v1`, and
`odlrq_exact_quotient_coordinate_generator_v1`, with non-erasable
`evidence_scope=synthetic_development` inherited from the admitted source and
`domain_scope=declared_finite_totalized_snapshot_only`.

Before construction, `ExactAdmissionCompletionGate` is rerun,
`VerifiedExactPartition` is freshly verified, and `ExactFiniteOperator` is
freshly exported.  Target/delta/replay/cap/censor semantics, complete closed
totalized domain, boundary identity, membership, frame, environment,
transition, response-vocabulary, partition, and operator digests are mandatory.
Failure exposes no exact generator.

With canonical block basis `e_C`, column-source orientation, and one ordered
action `a`, E0 defines only the structural discrete generator family

```text
Delta_a e_C = e_(tau_a(C)) - e_C.
```

`ExactQuotientCoordinateTerm(target_block_index, coefficient)` stores reduced
nonzero `ExactRational` coefficients.  `ExactQuotientTransferRow` binds source
block, action, structural target, complete member-transition count/digest, and
sorted terms.  `ExactQuotientCoordinateGenerator` is sealed and reconstructible
only from its retained exact source.  Self-loops normalize to no terms;
otherwise the unique terms are `-1` at the source and `+1` at the target.
Closed and sink blocks self-loop.  Member count is coverage evidence, never a
coefficient or multiplicity.

The wire binds all authority digests, `block_basis_column_source_v1`,
`P_action_minus_identity_v1`, canonical action/block orders, row/term counts,
and source seal.  There are exactly `B*A` rows.  Caps `B<=128`, `A<=16`,
`B*A<=2048`, nonzero terms `<=4096`, member/action witnesses `<=2048`, exact
rational pre-reduction bit length `<=8192`, and total work `<=250000` are checked
before any proportional allocation.

The fixed E0 fixture table is:

```text
g0-self:    blocks O0=[s0],CLOSED=[c0],SINK=[k0]; action [a]; O0-a->O0
g0-move:    blocks O0=[s0],O1=[s1],CLOSED=[c0],SINK=[k0]; action [a];
            O0-a->O1, O1-a->O1
g0-diamond: blocks O0=[s0],O1=[s1],O2=[s2],CLOSED=[c0],SINK=[k0];
            actions [a,b],
            O0-a->O1, O0-b->O2, O1-{a,b}->CLOSED,
            O2-{a,b}->CLOSED
g0-members: g0-move with O0 members [s0,s1], both mapped by a to O1;
            O1 has member [s2], every terminal block is singleton
common terminal rule: CLOSED and SINK self-loop for every action in every fixture
```

The total constructor grammar is also frozen.  It uses response vocabulary
`("block_index",)`, environment descriptor
`{"bundle":"u24","fixture":<displayed-name>}`, frame strings
`source_lane="u24_synthetic"`, `granularity="exact_block_member"`,
`normalization_id="generation_time_exact_v1"`, and
`extractor_version="u24_fixture_v1"`.  Every digest is uppercase SHA-256 of
strict canonical JSON of the displayed precursor.  State payload is exactly
`{"kind":"u24_state","name":state_id}`, action payload exactly
`{"kind":"u24_action","name":action_id}`.  Open response is its displayed
block index; CLOSED/SINK responses are `100/101`.  Expansion is `SEALED`,
boundary complete is true, truncated is false, live handle/censor are
`NOT_APPLICABLE`, all open states are seeds, and `SyntheticEvidenceProfile` is
the strict default.  Action-alphabet, semantics, seed, domain-payload, frame,
and vocabulary digests are derived by the existing strict factories from these
complete sorted arrays.  Transitions contain exactly state-count times
action-count rows; omitted open transitions are forbidden, not sampled.

These rules, names, orders, and displayed transition tables completely
determine the fixture bytes.  Tests use an
independent oracle for every delta, canonical round-trip/permutation, exact
cancellation, terminal rows, member-count non-scaling, stale/member/target/
digest mutations, duplicate/reordered/bool/float/unknown/subclass kills, caps,
and rejection of certified/observed/nominal sources.

An ordering regression asserts that E0 exports/imports no `FiberEnvelope`,
positive majorant, rate, probability, or nominal symbol; quotient normalization
therefore exists as a typed predecessor rather than a comment.

E0 emits no action rate, probability, fiber law, positive upper bound,
envelope, MaxEnt object, or continuous-time claim.  Exact structural terms may
combine only when their canonical coordinate and witness are identical.  The
symbols `alpha` and learner scores can never scale E0 coefficients.

Success: `CPU_SYNTHETIC_QUOTIENT_COORDINATE_GENERATOR_VERIFIED`.

## 9. E1 -- exact weighted finite-fiber envelope

E1 begins only after green E0 and changes only:

```text
lean_rgc/odlrq/quotient_generator.py
lean_rgc/odlrq/envelope.py
lean_rgc/odlrq/__init__.py
tests/test_odlrq_quotient_generator.py
tests/test_odlrq_envelope.py
tests/tier_manifest.json
```

It owns `WeightedCompression`,
`WeightedLifting`, `PositiveFiberWeights`, `ExactFiniteFiberLaw`,
`DeclaredSyntheticTransferLayer`, `FiberCompletenessWitness`,
`DomainMembershipWitness`, `FiberInclusionWitness`, and `FiberEnvelope`.
They live in `quotient_generator.py`/`envelope.py`; all public witnesses are
sealed, strict, source-bound, and independently rederived on serialization.

For source member `T` in block `Q`, target member `T'` in `Q'`, positive reduced
rational weights `omega`, and a nonnegative exact fiber probability law `mu`,
the fixed column-source formulas are:

```text
(C_omega x)_Q = sum_(T in Q) omega(T) x_T
(L_(mu,omega) z)_T = mu(T|Q) z_Q / omega(T)
K_mu = C_(omega,target) A L_(mu,omega,source)
M(Q',Q) = max_(T in Q) sum_(T' in Q')
          abs(A(T',T)) omega_target(T') / omega_source(T)
```

Every `omega(T)>0`; every member appears exactly once.  `mu(T|Q)>=0`, covers
each fiber exactly, and sums exactly to one in each fiber.  Therefore
`C_omega L_(mu,omega)=I`.  Zero transfer cells are absent from the canonical
sparse wire, but a complete declared rectangular key universe and its digest
make absence mean exact zero; duplicate cells are rejected.

`DeclaredSyntheticTransferLayer` binds distinct source/target exact domains,
partitions, coordinate orders, complete member sets, and a sorted exact
coefficient table, as well as separate source and target E0 generator digests
that fix both structural coordinate frames.  Its coefficient authority is
literally `DECLARED_SYNTHETIC_FIXTURE`; it is not reclassified as a Lean event-
derived operator.  E0 deltas are not silently treated as rates or transfer
coefficients.  A later production phase requires a new
`ExactTransferAdmissionGate` that rederives every coefficient from exact events.
Signed cancellation is permitted only before E1 when exact
duplicate coordinates and witnesses coincide.  It never reduces `M` after the
positive boundary.  `CertifiedIntervalOperator` remains a structural successor
containment capability and is not treated as a numeric positive operator.
Observed and nominal operators cannot construct any E1 witness or token.  The
E1 envelope is hard only relative to the displayed complete declared synthetic
table and cannot construct a production certificate.

The certificate stores, for every `(Q',Q)`, every candidate member load, the
lexicographically first maximizing member on ties, the maximum, member count,
and work count.  An independent verifier recomputes all loads and checks
`M(Q',Q)>=abs(K_mu(Q',Q))` coefficientwise.  This finite exact matrix
inequality proves `abs(K_mu z)<=M abs(z)` for every rational `z`; bounded vector
enumeration is a kill test only.

`FiberInclusionWitness` fixes identical quotient block IDs/order and injects
every old member/cell into the extended layer.  Monotonicity compares matrices
only through that typed injection.  An extension may preserve or increase each
cell and may never decrease one.

The fixed E1 fixtures are:

```text
T0-W:
  source/target members=[OPEN,CLOSED,SINK], singleton fibers,
  A=[[10,0,0],[0,0,0],[0,0,0]],
  omega_source=[100,1,1], omega_target=[1,1,1], mu=[1] in every fiber
two-fiber:
  source members=[s0,s1,s2,CLOSED,SINK],
  fibers Q0=[s0,s1], Q1=[s2], CLOSED=[CLOSED], SINK=[SINK]
  target members=[t0,t1,t2,CLOSED,SINK],
  fibers R0=[t0,t1], R1=[t2], CLOSED=[CLOSED], SINK=[SINK]
  both exact domains append CLOSED/SINK, use ordered action [a], make every
  open block self-loop under a, and use response coordinate equal to block index
  A is the complete 5x5 matrix with open 3x3 block
    [[1,-2,0],[0,1,1/2],[3,0,-1]] and every other cell exact zero
  omega_source=[1,2,1,1,1], omega_target=[2,1,3,1,1]
  mu_Q0=[1/3,2/3], mu_Q1=[1], mu_CLOSED=[1], mu_SINK=[1]
extension:
  source members=[s0,s1,s2,s3,CLOSED,SINK], inject two-fiber,
  add s3 to Q0 with complete target column [0,4,1,0,0],
  omega_source=[1,2,1,1,1,1] and mu_Q0=[1/6,1/3,1/2];
  all other weights/laws/zero cells are inherited exactly
```

T0-W records that the old formula gives envelope `0.1` against reduced gain
`10`, while corrected lifting gives gain `0.1`.  The fixture tables, canonical
orders, and exact rationals above are complete; no generated coefficient is
chosen after execution.

Caps are source/target members `<=128` each, blocks `<=64`, sparse transfer
cells `<=4096`, candidate loads `<=250000`, and the existing 8192-bit rational
cap, all checked before allocation.  Tests cover independent exhaustive
recomputation, weighted round-trip, missing member/extremizer, undercoverage,
representative substitution, outside domain, permutation, stale/mutated
authority, observed/nominal rejection, and both equality/strict extension.

Success: `CPU_SYNTHETIC_FIBER_ENVELOPE_CORE_VERIFIED`.  “Complete” and “hard”
mean only the declared finite synthetic domain, never a mathematical or Lean
all-germ fiber.

## 10. E2 -- finite horizon, pressure, return memory, and binding gate

E2 begins only after green E1 and changes only:

```text
lean_rgc/odlrq/envelope.py
lean_rgc/odlrq/selection.py
lean_rgc/odlrq/certificates.py
lean_rgc/odlrq/__init__.py
tests/test_odlrq_envelope.py
tests/test_odlrq_selection.py
tests/tier_manifest.json
```

With column vectors and consecutive compatible
layers, it defines

```text
M_[n,n+h) = M_(n+h-1) ... M_n
M_n^T w_(n+1) <= theta_n w_n,  w_n > 0
||M_[n,n+h)||_(1,w) <= product_(j=n)^(n+h-1) theta_j.
```

`CocycleCertificate` binds every domain/codomain, weight, layer digest,
inequality slack, multiplication order, horizon `1<=h<=16`, and exact product.
First- and second-moment channels are non-substitutable enum variants
`P1_BRANCHING_ADJUSTED` and `P2_BRANCHING_ADJUSTED`; each carries its own
matrices, weights, theta sequence, product threshold, and pass bit rederived by
the checker.  Log-pressure is a float diagnostic derived only after the exact
product decision.

For a declared retained `P` block and memory `Q` block, the finite positive
return-memory bracket is

```text
R_h = sum_(k=0)^(h-1) M_PQ (M_QQ)^k M_QP,
```

with each block orientation, split digest, weighted `1`-norm, term, and finite
sum stored.  It is no infinite-horizon or decay claim.

E2 prechecks quotient dimension `<=128`, horizon `<=16`, candidates `<=64`,
matrix cells per layer `<=16384`, return-memory terms `<=16`, and total exact
multiply/add work `<=250000` before allocating products, powers, token arrays,
or candidate records.

The lexicographic gate in `selection.py` consumes immutable tokens in this
order: frame/semantics, domain membership, E1 envelope, then E2 horizon.  It
constructs `CertifiedSupportToken` from exactly the candidates passing those
four pre-utility layers; ME0 and nominal utility consume that output token and
the token is never its own constructor input.  Every token binds candidate, source digests,
coverage denominator, and expiry/invalidation digest.  Reject and abstain are
distinct; all registered candidates stay in the denominator.  On nominal
failure, the fixed fallback order is ascending canonical candidate ID among
already-certified candidates only.

`CertifiedSupportToken` is the strict sorted tuple of candidate IDs that passed
all four pre-utility layers, plus gate/envelope/horizon/frame/domain digests,
coverage numerator/denominator, and evidence/domain scopes.  It is constructed
only by the gate and its serializer replays every decision; ME0 cannot add an
atom.

Fixed E2 fixtures use

```text
M0=[[1,2],[0,1/2]], M1=[[1/2,0],[3,1]], horizon=2
P1 weights w0=w1=w2=[1,1], theta=[5/2,7/2], product threshold=35/4
P2 matrices are entrywise squares of M0/M1,
   weights=[1,1], theta=[17/4,37/4], product threshold=629/16
nonnormal=[[1,10],[0,1]], nilpotent=[[0,1],[0,0]]
return blocks M_PQ=[2], M_QQ=[1/2], M_QP=[3], h=3
candidates [(c0,bound=1,utility=1),(c1,bound=3,utility=9),
            (c2,bound=2,utility=4)], hard bound threshold=2
```

The fixture must accept `c0,c2`, reject `c1`, and rank `c2` before `c0`, while
the ungated utility ranking starts with rejected `c1`.  Thus the gate is
reachable both ways, nonempty, and ranking-binding.  E2 tests also cover P1/P2
non-substitution, transient gain, token invalidation, coverage, fallback, and
every matrix-order mutation.

Success: `CPU_SYNTHETIC_FINITE_HORIZON_ENVELOPE_VERIFIED`.

## 11. ME0 -- MaxEnt model selection inside fixed support

ME0 begins only after green E2 and changes only:

```text
lean_rgc/odlrq/maxent.py
lean_rgc/odlrq/__init__.py
tests/test_odlrq_maxent.py
tests/tier_manifest.json
```

It requires the exact digest of an E2 support
token.  The reference law is strictly positive on every supported atom;
orbit-corrected reference mass is proportional to `q_i/|Orbit(i)|` and is
renormalized before fitting.  For sufficient statistic `phi_i`, target `m`, and
dual parameter `lambda`, it uses

```text
p_i(lambda) = q_i exp(lambda dot phi_i - log Z(lambda))
KL(p||q) = sum_i p_i log(p_i/q_i).
```

Support `<=32`, statistic dimension `<=4`, orbit size `<=32`, and exact hull
subset work `<=242824` are prechecked; this is
`sum_(k=1)^5 binom(32,k)`, including the required `d+1` subsets.  Exact
reduced-rational affine-rank and
Caratheodory-subset enumeration classify in the fixed order: malformed input is
a hard error; affine rank below declared dimension gives
`SINGULAR_STATISTICS`; otherwise outside hull gives `OUTSIDE_HULL`; a hull-boundary
target gives `BOUNDARY_NO_FINITE_PARAMETER`; only an interior target enters the
numeric solver and returns `INTERIOR_SOLVED` or `NUMERIC_FAILURE`.

The sole backend is NumPy float64 log-sum-exp plus damped Newton/pseudoinverse:
zero initial lambda, `rcond=1e-12`, at most 128 iterations, at most 32 halving
backtracks per iteration, first accepted step on ties, moment infinity-norm
`<=1e-10`, simplex error `<=1e-12`, dual residual `<=1e-10`, and no nonfinite
intermediate.  Exhaustion/nonfinite is `NUMERIC_FAILURE` and is never relabelled
boundary/outside.  Test-only internal configuration with zero iterations
exercises that branch but cannot be constructed by the public endpoint.
Every finite float output is serialized as a canonical 17-significant-digit
decimal string and reparsed to the identical binary64 value; raw JSON floats,
NaN, infinity, negative zero, and locale-dependent formatting are forbidden.

Fixed fixtures use the E2 token `support=[c0,c2]`, statistics `[0,2]`, reference
`[1/2,1/2]`, orbit sizes `[1,1]`, interior target `1`, boundary target
`0`, outside target `3`, singular statistics `[[1],[1]]`, KL radius `1/20`, and
operator-span residual threshold `1e-10`.  The checker records primal, dual,
simplex, moment, KL, orbit, support, and registered-rule-span residuals.
For vectorized nominal operator `k` and frozen exact-rule columns `G`, the span
residual is `min_beta ||k-G beta||_2` using the same fixed pseudoinverse.  For a
nonnegative row load `r` and KL radius `eta`, the reported robust diagnostic is
the frozen Pinsker bound
`E_p[r] <= E_q[r] + (max(r)-min(r))*sqrt(eta/2)`; it cannot exceed or replace
the E2 envelope token.
MaxEnt may choose a nominal law but creates no safety token and cannot enlarge,
shrink, or replace the certified feasible set.

Success: `CPU_SYNTHETIC_MAXENT_CORE_VERIFIED`.

## 12. S0 -- typed global similarity with finite-level remainder

S0 begins only after green ME0 and changes only:

```text
lean_rgc/odlrq/similarity.py
lean_rgc/odlrq/__init__.py
tests/test_odlrq_similarity.py
tests/tier_manifest.json
```

`LocalTower` stores the fixed nested `(R,N,G)` levels and a deterministic
fine-to-coarse restriction at every adjacent pair; missing levels, multiple
restrictions, or a noncommuting identity/composition diagram fail closed.
`PredictiveDistance` and `PositiveDistance`
are disjoint final types.  `GlobalMeasure` stores nonnegative node mass `m1`,
directed edge mass `m2`, totals `rho1/rho2`, explicit normalization witness, and
cross-covariance residual.  Zero-total measures compare only with another
zero-total measure; zero/nonzero comparison fails closed rather than silently
normalizing.

For explicitly normalized measures the base representation objects are

```text
PredictiveMetric(x,y) = (1/2)||m1_x-m1_y||_1
                        + (1/2)||m2_x-m2_y||_1
PredictiveDiscrepancyUpperBound = PredictiveMetric
                        + r_cross,x + r_cross,y + r_numeric,x + r_numeric,y
PositiveRepresentationDistance = ||D_(L_plus)(m1_x-m1_y)||_1
                        + ||D_(L_plus,edge)(m2_x-m2_y)||_1
SafetyMajorant = PositiveRepresentationDistance
                        + e_target(x) + e_target(y).
```

Only `PredictiveMetric` is required to satisfy metric identity; the discrepancy
upper bound is a different type and may be positive at `(x,x)`.  The positive
representation distance is exact/certified rational.  `SafetyMajorant` requires
an S0 `DeclaredSyntheticLPlusToken`, both hard target residuals, and coverage.  Learned
`L_plus`, omitted target residual, and zero sampled violations cannot construct
it.

`DeclaredSyntheticLPlusToken` binds the E1 envelope digest, E2 cocycle digest,
the exact node/edge weights, evidence/domain scopes, and complete S0 fixture
universe.  Its independent checker enumerates that universe and recomputes the
declared node/edge Lipschitz upper loads.  `D_(L_plus)` is exactly the diagonal
matrix of the listed weights.  This token is declared-synthetic and cannot be
promoted to a production hard `L_plus`.

Predictive float fields use the ME0 canonical binary64 decimal-string rule;
positive/certificate fields use reduced `ExactRational` only.

`ApproximationLevelId=(frame,domain,R,N,G)` owns exactly one of
`RadiusMorphism`, `WordDepthMorphism`, or `GranularityMorphism`.  Each fine-to-
coarse map is exact nonnegative column-stochastic, binds source/target levels,
node/edge transport, coverage numerator/denominator, commutator `1`-norm,
target residual, cross-covariance budget, and exact remainder `e`.  Its checker
verifies

```text
U_coarse(Px,Py) <= U_fine(x,y) + e,
```

where `U` is explicitly either `PredictiveDiscrepancyUpperBound` or
`SafetyMajorant`, never the other.  For the predictive channel, `e` includes
both recorded endpoint numeric residuals; they are never silently rounded away.

Identity has `e=0`; composition preserves axis/order and verifies
`e_composite<=e_first+e_second`.  The fixed nested schedule is
`(1,1,1)->(2,1,1)->(2,2,1)->(2,2,2)`, changing R, then N, then G.  Its finite
Cauchy majorant is the exact nonincreasing sequence `[3/4,1/2,1/4,0]`; every
later-pair distance transported to an earlier level must be below the earlier
remainder.  This is a finite registered remainder certificate, not an
infinite-cutoff theorem.

The fixed hard positive weights are `L_plus,node=[1,2]` and
`L_plus,edge=[1,1,2]`; every complete case has hard target residuals
`e_target(x)=e_target(y)=1/8` and coverage `4/4` unless marked otherwise.  The
fixed node alphabet is `[n0,n1]`, edge alphabet is
`[(n0,n0),(n0,n1),(n1,n1)]`, and the exact case table is:

```text
s-id:       x=y; node=[1,0], edge=[1,0,0], cross=0, coverage=4/4, map=I
s-node:     x.node=[1,0], y.node=[0,1], both edge=[1,0,0]
s-edge:     both node=[1/2,1/2], x.edge=[1,0,0], y.edge=[0,0,1]
s-cross:    masses equal to s-id, x.cross=1/8, y.cross=0
s-cover:    masses equal to s-id, coverage=3/4
s-zero:     x=y with zero node/edge mass
s-zero-kill:x zero, y equal to s-id
s-compose:
  levels L0=(1,1,1), L1=(2,1,1), L2=(2,2,1), L3=(2,2,2)
  fine-to-coarse order L3 -G-> L2 -N-> L1 -R-> L0
  P_R,node=I2; P_R,edge=I3
  P_N,node=I2; P_N,edge=I3
  P_G,node=J=[[1,1],[0,0]];
  P_G,edge=[[1,1,1],[0,0,0],[0,0,0]]
  at L3: x.node=[1,0], y.node=[1/2,1/2],
         x.edge=[1,0,0], y.edge=[1/2,1/2,0]
  at each lower level the measures equal the exact image under its displayed P
  per-map remainder=[1/4,1/4,1/4], Cauchy majorant=[3/4,1/2,1/4,0]
  all level coverages=4/4; all x/y target residuals=1/8
```

All unspecified cross residuals are zero and coverage is `4/4`.  Edge maps are
the canonical maps induced by the displayed node maps; ambiguity or a missing
edge image fails.  Enumeration order is the displayed case/level/node/edge
order.  Tests cover restriction/composition, node-only aliasing, edge
separation, covariance inflation, zero mass, coverage, target residual, all
axes, and predictive/positive type substitution.

Success: `CPU_SYNTHETIC_TYPED_SIMILARITY_CORE_VERIFIED`.

## 13. I0 -- integrated CPU candidate, artifacts, and closeout

I0 changes only:

```text
lean_rgc/odlrq/certificates.py
lean_rgc/odlrq/__init__.py
lean_rgc/evals/uprime_u2_u4_development.py
tests/test_uprime_u2_u4_development.py
tools/run_uprime_u2_u4_development_tests.ps1
tests/tier_manifest.json
```

It remains independent of `action_geometry.py`, native Lean, official
transport, and every production selector.  For typed stages `F_i:X_i->X_(i+1)`
with norm-Lipschitz constant `L_i`, residual `epsilon_i`, initial residual
`e_0`, and complete coverage, `TypedPipelineBound` verifies independently:

```text
e_final <= (product_i L_i) e_0
           + sum_i (product_(j>i) L_j) epsilon_i.
```

Every factor stores domain, codomain, norm, evidence scope/tier, authority
digests, coverage, and exact multiplication order.  Missing coverage abstains;
nominal residuals remain nominal terms and cannot be promoted into the hard
sum.  A producer and a separate strict checker must agree byte-for-byte.

The fixed integrated pass case is:

```text
stage order:
  E0 quotient normalization: L=1,   epsilon=0,   tier=exact
  E1 envelope transport:     L=2,   epsilon=1/8, tier=exact-declared-synthetic
  E2 finite horizon:         L=3/2, epsilon=1/4, tier=certified-synthetic
  ME0 nominal lifting:       L=1,   epsilon=1/10,tier=nominal
  S0 level transport:        L=1,   epsilon=1/8, tier=certified-synthetic
initial residual e0=1/16; coverage=4/4
expected hard bound=3/4; nominal addendum=1/10; total reported bound=17/20
hard threshold=3/4; PASS iff hard_bound <= threshold;
FAIL iff hard_bound > threshold
```

The hard bound excludes the ME0 nominal epsilon and independently recomputes
the displayed `3/4`; the total report appends but never promotes `1/10`.  The
fixed fail case changes only E1 epsilon to `1/4` and must exceed the hard
threshold with recomputed hard bound `15/16`.  The fixed abstain case changes only S0 coverage to `3/4` and emits
no bound.  The candidate manifest is the ordered tuple
`[E0.generator_sha256,E1.envelope_sha256,E2.cocycle_sha256,
E2.support_token_sha256,ME0.result_sha256,S0.similarity_certificate_sha256]`
plus `runtime_identity_sha256` and the exact stage labels above; no candidate is
selected by outcome.

The one artifact-emission command is:

```powershell
& .\tools\run_uprime_u2_u4_development_tests.ps1 -Lane EMIT
```

The I0 source lane qualifies the same integration in memory and temporary
runner-owned bytes, deletes those bytes, and does not claim tracked artifact
identity.  The displayed emission runs once after I0 source candidate/accepted
CI is green, refuses any
pre-existing output, activates the guard before importing
`lean_rgc.evals.uprime_u2_u4_development`, passes the sole fixed output root
internally, and emits all stage objects in one capped child process.  Only the
CLOSEOUT lane validates those tracked
bytes and it does not regenerate them.  Publication/closeout never reruns the
semantic lane.

Every artifact is canonical strict JSON with common fields exactly
`schema_version`, `evidence_scope`, `source_commit`, `observation_frame_id`,
`reachable_domain_id`, `response_vocabulary_id`, `transition_semantics_id`,
`domain_scope`, `runtime_identity_sha256`, `operator_tier`, `operator_sha256`,
`coverage`, `censors`, `disposition`, and
`payload`.  Schemas are respectively `u24_envelope_core_v1`,
`u24_maxent_fixture_v1`, `u24_local_tower_v1`, `u24_global_measure_v1`,
`u24_level_transport_v1`, `u24_similarity_certificate_v1`, and
`u24_integrated_certificate_v1`.

The payload key sets and their listed order are fixed:

```text
envelope_core:
  generator,source_weights,target_weights,fiber_law,transfer_layer,
  completeness_witness,inclusion_witness,envelope,verification_report
maxent_fixture:
  support_token,reference_law,orbit_law,statistics,target,kl_radius,status,
  probabilities,residuals,verification_report
local_tower:
  levels,restrictions,cauchy_majorant,verification_report
global_measure:
  measures,predictive_metric,predictive_upper_bound,
  positive_representation_distance,safety_majorant,verification_report
level_transport:
  radius_morphism,word_depth_morphism,granularity_morphism,composition,
  verification_report
similarity_certificate:
  coverage,target_residuals,l_plus_token,remainder_certificate,
  verification_report
integrated_certificate:
  candidate_manifest,stages,initial_residual,hard_bound,nominal_addendum,
  total_bound,coverage,disposition,verification_report
```

Each value is a strict schema-tagged object or an array in the displayed
canonical order; no payload delegates its field set to a future unconstrained
`to_dict()`.  Unknown/missing/reordered fields fail.  The non-erasable scopes
are `evidence_scope=synthetic_development` and
`domain_scope=declared_finite_totalized_snapshot_only`.

Success: `CPU_SYNTHETIC_U2_U4_CANDIDATE_CONSTRUCTED`.

The consolidated closeout ref adds only:

```text
docs/experiments/uprime_odlrq_u2_u4_development_construction_closeout_2026-07-13.md
docs/experiments/artifacts/uprime_u2_u4_development_20260713/envelope_core.json
docs/experiments/artifacts/uprime_u2_u4_development_20260713/maxent_fixture.json
docs/experiments/artifacts/uprime_u2_u4_development_20260713/local_tower.json
docs/experiments/artifacts/uprime_u2_u4_development_20260713/global_measure.json
docs/experiments/artifacts/uprime_u2_u4_development_20260713/level_transport.json
docs/experiments/artifacts/uprime_u2_u4_development_20260713/similarity_certificate.json
docs/experiments/artifacts/uprime_u2_u4_development_20260713/integrated_certificate.json
```

If a stage stops before emission, no absent artifact is synthesized; the fixed
failure document alone records the terminal label.  A closeout records every
commit/CI, Windows command, receipt, wall/RSS, test count, fixture digest,
failure, and nonclaim.  The success closeout requires distinct green candidate
and accepted CI.  Candidate-closeout CI red leaves accepted at the last
semantic commit and creates F with that parent, citing but not importing
candidate artifacts.  Accepted-closeout CI red creates F with the immutable
accepted closeout as parent.  Any earlier stage failure creates F from the last
accepted semantic commit.  If F candidate or accepted CI is red, the phase
stops without repair or recursion.

## 14. Stop rules and nonclaims

The bundle stops and closes at the first of:

- an exact/certified/observed/nominal tier crossing;
- incomplete fiber/domain coverage or stale authority;
- a false `C_omega L_(mu,omega)=I` or majorant inequality;
- a fiber extension that decreases the envelope;
- a nominal object constructing or bypassing a hard token;
- predictive/positive distance substitution;
- an untyped or unbounded R/N/G transport;
- an unregistered path, attempted ninth build commit, second correction commit, two
  consecutive red candidates,
  resource cap, protected read, native Lean/transport call, network/SSH/GPU/LLM
  capability, or outcome-dependent fixture/cap change.

The terminal failure labels are closed:

```text
U24_A0_CI_BLOCKED
U24_B0_IDENTITY_OR_RUNNER_BLOCKED
U24_EXACT_ADMISSION_BLOCKED
U24_E0_GENERATOR_BLOCKED
U24_E1_ENVELOPE_BLOCKED
U24_E2_ENVELOPE_BLOCKED
U24_E2_GATE_NONBINDING
U24_ME0_MAXENT_BLOCKED
U24_S0_SIMILARITY_TRANSPORT_BLOCKED
U24_I0_INTEGRATION_BLOCKED
U24_RESOURCE_OR_SCOPE_BLOCKED
U24_CLOSEOUT_CI_BLOCKED
```

An A0 candidate-CI failure is parented to `78c1254...`; an A0 accepted-CI
failure is parented to immutable A0; any later pre-source failure is parented to
accepted A0; and a stage failure is parented to the last accepted semantic
commit.  Closeout-CI failure follows section 13.  No failed stage is rerun,
weakened, relabelled, or continued under this authority.

Failure is preserved as a development result and is not evidence that the
upper mathematical program is impossible.  No outcome here claims protected
K1--K4 performance, same-family D4 rank, production Lean locality, complete
all-germ quotient, production hard envelope, MaxEnt safety, global Lean
similarity, learner improvement, solve rate, deployment, remote/GPU work, or
LLM benefit.

After a successful I0 closeout, the next order is fixed:

```text
synthetic U'1.5-L0 registration may proceed independently
one final bounded public-synthetic official-transport qualification
  -> filled Amendment A for the exact CPU candidate
  -> protected K-series only if Amendment A is pushed and green
  -> native Lean-oracle U'1.5-L1 registration
  -> optional GPU learned representation
  -> optional LLM knowledge distillation last
```

Until that separate transport qualification succeeds, Amendment A's
environment/transport fields remain `UNFILLABLE / UNFREEZABLE`; this bundle is
not a substitute.

## 15. Learner and deferred-item registration routes

This bundle creates no learner, but reserves the only next learner authority:

```text
document docs/experiments/uprime_odlrq_u15_l0_locality_cegar_amendment_2026-07-13.md
A0 ref   codex/uprime-u15-l0-locality-cegar-a0
build    codex/uprime-u15-l0-locality-cegar-build
```

It may be drafted only after successful I0 closeout.  Synthetic L0 may consume
only a newly frozen task/query matrix and before-only features.  A query binds
`BehavioralObservationKey`, `ActionSymbol`, boundary/mvar-sharing graph,
proposed class, query cost, and source digests.  An oracle event binds replayed
before/action identity, totalized response, censor, and exact witness.  An exact
counterexample may only split a class; absence of a counterexample remains
statistical, and a learner merge never constructs an exact partition, fiber
completeness witness, positive envelope, or safety token.  The frozen proper
loss is conditional covariance reduction per query cost, with coverage,
abstention, seed, tie-break, and baseline reported.  L0 success is only
`CPU_SYNTHETIC_LOCALITY_CEGAR_PROPOSER_VERIFIED`; native Lean L1 remains barred
until separate official transport qualification.

Occurrence-targeted rewrite and macro decomposition remain separately reserved:

```text
docs/experiments/uprime_odlrq_occurrence_rw_site_api_amendment_2026-07-13.md
ref codex/uprime-occurrence-rw-site-api-a0
start only after target routing, occurrence identity, read/write support,
and F2 response-separation fixtures are frozen

docs/experiments/uprime_odlrq_opaque_macro_hyperedge_amendment_2026-07-13.md
ref codex/uprime-opaque-macro-hyperedge-a0
start only after decomposition semantics and replay-equivalent macro traces
are frozen; otherwise macros remain audited opaque hyperedges
```

Only after the serial I0 bytes are green may three separate performance phases
be drafted, in this order:

```text
codex/uprime-u24-parallelization-a0
  orbit / interface-grade / separator sharding;
  serial and sharded canonical bytes must agree
codex/uprime-u24-continuousization-a0
  finite-quotient Poisson generator and sparse derivation;
  discrete/continuous finite-horizon transport error remains explicit
codex/uprime-u24-gradientization-a0
  nominal float coefficients and acquisition loss only;
  exact structure, hard upperness, and certificates remain CPU-rechecked
```

None is licensed by this document.  Parallelization cannot alter semantics,
continuousization cannot create an infinite-horizon claim, and gradients cannot
touch exact rational structure or a hard decision.

LLM proposal/distillation has no ref in this plan.  It remains after the exact
generator, upper pipeline, and theory-driven locality proposer work, under a
future contamination/model-runtime amendment.

## 16. Adversarial implementation-plan review

The 2026-07-10 47-agent mathematical review is not repeated.  Before A0 is
committed, independent reviewers attack this implementation mapping for:

1. authority/order and protected-data leakage;
2. exact weighted algebra, fiber completeness, tier firewall, and finite-
   horizon counterexamples;
3. MaxEnt support/safety confusion and numeric failure;
4. typed similarity transport, coverage, residual, and approximation stability;
5. path/commit/resource budgets, failure topology, and anti-fractal limits.

Three independent implementation reviewers attacked the first concrete draft
on 2026-07-13.  After overlap reduction, twelve finding clusters were confirmed
and none was refuted: generator/envelope collapse, missing mu/omega and matrix
orientation, ill-typed fiber extension, universal-vector test ambiguity,
unfrozen fixtures, result-dependent walls, missing preallocation caps, missing
exact-admission boundary, underdefined E2/MaxEnt/similarity/telescoping
certificates, ambiguous artifact publication, and missing learner/deferred-item
routes.  Sections 4--15 incorporate the repairs.

Two further adversarial passes confirmed and repaired: declared-transfer
authority, complete terminal matrices and fixture constructor grammar,
Caratheodory work count, noncyclic support-token construction, metric-versus-
residual types, explicit `L_plus`/target residual/R-N-G maps, integrated
thresholds and artifact schemas, narrow D4-order supersession, exact
candidate/accepted topology, machine-enforced no-read prefixes, correction
scope, EMIT/CLOSEOUT timing, WP9 artifact decomposition, and the complete
Python/NumPy/OpenBLAS runtime identity.

Final independent dispositions on the repaired bytes:

```text
c2_engine           APPROVE
e1_identity_check   APPROVE
e1_governance_audit APPROVE
```

**Final implementation-plan disposition: APPROVE for this exact A0 freeze.**
A reviewer cannot license source work; only the exact committed, pushed, green
A0 on both candidate and accepted refs can.
