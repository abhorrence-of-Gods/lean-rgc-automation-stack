# U-prime / ODLRQ U2--U4 R2 one-time topology bootstrap amendment

Date: 2026-07-13 (Asia/Tokyo)

Status: **ONE COMPOSITE PRE-SCIENTIFIC T0 COMMIT; FROZEN ONLY AFTER THE T0
HEAD IS GREEN ON ITS CANDIDATE REF AND ON THE ACCEPTED BRANCH**

Immediate parent: immutable red A2 commit
`7c05c494ce79e84ffeb0d0c912ca3ba5f141f402`.

Accepted base: R1 exact-admission failure closeout
`1d448a2322b639b462d8cda8d20b4aaa55be232f`.

This document and the three control-file changes registered below are one
composite T0 commit.  The same-commit registration is a one-time control-plane
bootstrap exception: a doc-only successor is deterministically rejected by
the old terminal topology, while no mathematical source, fixture, protected
result, or scientific endpoint is touched.  The document bytes and all three
control-file bytes are adversarially reviewed before the single commit, push,
or CI run.

### 2026-07-13 red-CI clarification for future readers

The immutable U05 result-publication commit
`cc91a4181a9f87ec10f11727ed787eb7149f955a` has red Actions run `29166670576`
(job `86580832840`).  Dated audits found an identity-guard shallow-history
design omission, not a probe, prerequisite, artifact-integrity, or scientific
failure.  The exact scientific candidate
`3bb3408afc50a08307cff2c9b1906a299739dfb5` had green run `29166073728` (job
`86579287017`).  That red result badge must not be read as a failed scientific
execution.  T0 neither rewrites nor reruns the consumed endpoint.

## 1. A2 candidate result and adjudication

A2 added only
`docs/experiments/uprime_odlrq_u2_u4_development_r2_exact_admission_integration_amendment_2026-07-13.md`
to accepted base `1d448a2...`.  Its exact identities are:

```text
commit  7c05c494ce79e84ffeb0d0c912ca3ba5f141f402
parent  1d448a2322b639b462d8cda8d20b4aaa55be232f
blob    a6fb65eb7a3d52bfbae985eb6bf32bf5275ac3e1
```

A2 candidate CI run `29228439065`, job `86747336553`, passed Install,
Runtime boundary, and Dead candidate ledger, then ended with exactly:

```text
1 failed, 2580 passed, 8 skipped, 161 deselected
```

The sole failure was
`test_u24_b0_anchor_contiguous_budget_and_terminal_topology`, at
`_semantic_rows` line 303.  It reproduced on Windows at the same assertion.
The old A1-anchored first-parent sequence was:

```text
7377119 A1
  -> 7083e76 B0R
  -> 48c9127 B0R correction
  -> 1d448a2 accepted R1 failure terminal
  -> 7c05c49 A2 document
```

The old classifier removes a terminal row only when it is the last row.  Once
A2 follows, the exact accepted R1 terminal becomes an interior row and is
rejected before the A2 document, fixtures, formulas, or digests are read.  The
red run is therefore `CONTROL_TERMINAL_EPOCH_NOT_REOPENABLE`, not an E0
admission, generator, theory, or scientific failure.

Independent pre-push audits had already recomputed all four A2 fixture
environment/frame/snapshot/operator goldens, canonical block tables, delta
oracles, and work counts and found them exact.  T0 changes none of those bytes.
The red A2 ref `codex/uprime-u2-u4-development-r2-a0` remains immutable.

## 2. Exact supersession and activation event

T0 narrowly supersedes:

1. A2's requirement that commit `7c05c49...` itself have green candidate and
   accepted CI;
2. R1's permanent-terminal interpretation, only for the exact accepted R1
   failure commit/path/blob and the exact A2 child registered above; and
3. B0R immutability, only for the three existing control files and exact
   changes registered in sections 4--6.

A2's semantic document becomes frozen only when T0, or its one permitted
control-only correction child, passes candidate CI, the accepted branch is
fast-forwarded exactly from `1d448a2...` through the preserved red A2 commit to
that identical T0 head, and a distinct accepted CI is green on the identical
SHA/tree.  No E0 source may exist before that accepted run.

Every unlisted A2/R1/original clause remains in force.  T0 does not change an
E0--I0 path set, fixture, source golden, formula, schema, cap, wall, endpoint,
protected-read boundary, or post-success order.

## 3. Refs, path set, and commit budget

The only bootstrap ref is:

```text
T0       codex/uprime-u2-u4-development-r2-topology-bootstrap
accepted codex/uprime-odlrq-plan
```

After T0 acceptance, the already registered R2 refs remain:

```text
build    codex/uprime-u2-u4-development-r2-build
closeout codex/uprime-u2-u4-development-r2-closeout
failure  codex/uprime-u2-u4-development-r2-failure-closeout
```

T0 is the sole child of red A2 and changes exactly four paths:

```text
docs/experiments/uprime_odlrq_u2_u4_development_r2_topology_bootstrap_amendment_2026-07-13.md
tests/test_uprime_u2_u4_development.py
tests/uprime_u24_guard.py
tools/run_uprime_u2_u4_development_tests.ps1
```

It may not change the workflow, tier manifest, denylist, setup-residue
contract, runtime manifest, lane list, wall, disposition, test-function set,
scientific module, fixture, artifact, or external file.  It adds no new test,
runner, workflow, schema, ledger, CAS, publisher, recovery framework, or
generic epoch facility.

T0 is a bootstrap stage, not a correction and not a scientific B0 stage.  At
most one correction child exists across T0 and all later R2 stages.  A T0
correction may change only the three existing control files, never this
document.  If used, E0--I0 have zero correction budget left.  A document/spec
error, path expansion, non-control failure, second correction, red correction,
count drift, or accepted-branch drift stops without recursive repair.

The semantic order after accepted T0 remains:

```text
E0 -> E1 -> E2 -> ME0 -> S0 -> I0
```

There are at most seven semantic build commits: six named stages plus the sole
correction if it was not consumed by T0.  No new B0 row is inserted.

## 4. Identity epoch re-anchor

`tests/test_uprime_u2_u4_development.py` re-anchors its control attestation at
exact accepted R1 terminal `1d448a2...`.  That anchor has parent `48c9127...`,
tree `4442cff7b0c84fd5d24dd4d68020f5afade9cd12`, and exact historical blobs:

```text
identity  b4e67b08c1d6695c90850317740b10dd9a6001b4
guard     b877884528bc917c4cbeca48bd55223fe890f9e4
runner    61773201f5128d64dd9d17916f66c835569bf329
manifest  47997a9bd67f7ac2cf0cd9c7f654d8f069f925d8
workflow  0349e9ca864b7e9e25064ab7fab5b6dc0665adee
R1 terminal document
          8c1a048249b9ac2ddb0fa1ebc74eeac03fdd0692
```

The classifier admits exactly one new epoch:

```text
anchor 1d448a2
  -> exact A2 row 7c05c49
     parent=[1d448a2]
     changed={A2 document}
     A2 document blob=a6fb65eb...
  -> exact T0 row
     parent=[7c05c49]
     changed={this document, identity, guard, existing runner}
  -> optional sole T0 correction on the three control files
  -> E0 -> E1 -> E2 -> ME0 -> S0 -> I0
  -> optional registered R2 terminal
```

The T0 commit SHA is derived by Git and is not self-asserted.  Its document
blob is frozen in the identity file after this document's bytes are final.
The exact T0 changed set, sole parent, and tree blobs are mandatory.  After
T0, the resulting guard, runner, and workflow blobs may not change except for
the one registered T0 correction.  At I0 only the identity file's already
marked I0 extension body may change; its frozen core remains exact.  The
workflow remains byte-identical throughout.

The identity constants and tests are updated as follows:

- anchor, authority, A2, bootstrap, R2 closeout, and ref constants use the
  exact commits/paths/blobs registered here;
- `STAGE_ORDER=(E0,E1,E2,ME0,S0,I0)`, with no B0 semantic stage;
- `MAX_BUILD_COMMITS=7` and `MAX_CORRECTIONS=1`;
- the baseline manifest is the accepted anchor's `47997a9b...` blob, which
  already contains the B0R identity test;
- `TRACKED_PATHS` includes A2, this bootstrap document, both R2 closeouts, all
  inherited authorities, the existing union, and success artifacts;
- `ABSENT_AT_ANCHOR` removes the already-present identity, guard, and runner
  and includes only paths actually absent at `1d448a2...`;
- repository-wide `identity_additions` remains the exact historical singleton
  `[7083e766acd2ba09b45ba3f47f65dc0b34317bd3]`; it is verified as accepted
  B0R provenance but is never used to invent or classify an R2 B0;
- dirty T0 iteration is legal only when the governed dirty set is the exact
  four-path T0 set; after clean T0, pending dirt follows only the original
  E0--I0 allowlists and markers; and
- the five existing top-level test function names remain exact; topology
  attacks are loops/helpers inside the existing topology test.

The classifier verifies the accepted R1 terminal anchor and exact A2 bridge,
not a generic `failure -> reopen` pattern.  A success terminal, another
failure SHA/path/blob, duplicate bridge, or second reopen can never qualify.

## 5. Guard hash-only semantic preservation

`tests/uprime_u24_guard.py` changes only what is required to bind the new
control bytes:

```text
FROZEN_IDENTITY_CORE_SHA256 = SHA256(new identity core)
FROZEN_RUNNER_SHA256        = SHA256(new LF-normalized runner bytes)
static required literal     = MAX_BUILD_COMMITS = 7
```

The identity-core hash is computed from raw LF bytes with only the existing
I0 extension body excised between its frozen markers.  The runner hash is
computed after CRLF-to-LF normalization and bare-carriage-return rejection.
Runner and identity are finalized before these two hashes are written into the
guard, avoiding a hash cycle.

No other guard constant or behavior changes.  In particular control schema
v2, `DENYLIST_ROWS`, `CI_SETUP_PATHS`, setup-residue validation,
`UNION_SOURCE_PATHS`, workflow hash, path/process/network denial, static import
rules, and EMIT/CLOSEOUT modes remain byte-for-byte or semantically identical
as applicable.

The identity test supplies an independent normalized-delta witness rather than
trusting the new hashes merely because they are stored in the changed guard.
It replaces exactly the two new frozen hash values and the sole required
`MAX_BUILD_COMMITS = 7` literal with their anchor values and requires raw
SHA-256
`34880CA312F411FB526843A16A4722D5A19EADEE04C25402977A962DA3F5717C`,
the content hash of anchor guard blob `b8778845...`.  Wrong replacement counts
or any other guard-byte change fails.

## 6. Existing-runner re-anchor

The existing PowerShell runner is not replaced or forked.  Its three hardcoded
control/publication anchors become `1d448a2...`.  Its embedded tracked tuple
adds A2, this bootstrap document, and the R2 success/failure closeout paths.
Its absent-at-anchor tuple is recomputed against `1d448a2...`, removing the
already-present identity, guard, and runner while retaining truly absent
future scientific modules/tests and the new documents.

During dirty T0 qualification only, lane B0 adds this bootstrap document to
the otherwise unchanged allowed-dirty union.  No later lane or committed run
gets that exception.  The runner keeps its exact lane list, scientific stage
markers, runtime manifest, Python/NumPy/OpenBLAS identities, one-process Job,
2-GiB memory cap, 64-MiB output cap, walls, receipt schema, denylist copy,
plugin-autoload disablement, and dispositions.

B0 remains an identity diagnostic command, not an R2 build stage.  E0 remains
the first mathematical lane.

The identity test also reverses every registered runner edit with exact
counted replacements: the three anchor substitutions, the added tracked and
absent document entries, removal of the three no-longer-absent control files,
and the one B0-only bootstrap-document dirty exception.  After reversal and
CRLF-to-LF normalization, the runner must hash to the accepted anchor value
`38A3C357413FC3444034556AA675D9723D2B22E0E2B5A519ABDE935E4BD60DD1`.
Any simultaneous weakening of runner bytes and its new frozen hash therefore
still fails this independent base-delta witness.

## 7. Required attacks and qualification

Before the composite T0 commit, dirty-worktree qualification requires the
existing direct identity suite and:

1. exact golden `1d448 -> 7c05 -> pending/committed T0 -> E0...` topology;
2. rejection of missing/reordered/duplicate A2 or T0, wrong parent, merge,
   inserted commit, extra T0 path, mutated document blob, or changed E0 base;
3. rejection of a success-terminal reopen, another failure terminal, generic
   failure reopen, second bootstrap, or post-R2-terminal reopen;
4. rejection of workflow/manifest/denylist/setup/semantic changes in T0;
5. rejection of skipped/out-of-order E0--I0, cross-stage correction paths,
   reused R1 corrections, or two R2 corrections;
6. preservation of shallow-history, wrong-head, duplicate-revision,
   first-parent omission/reorder, malformed-control, forged-pyc, setup-residue,
   plugin-autoload, process/network, and protected-read attacks; and
7. exact identity-core/runner/workflow hashes, LF/CRLF normalization, no bare
   carriage return, normalized guard/runner base-delta witnesses, unchanged
   function set, and unchanged denylist cardinality.

Pre-push qualification requires:

```powershell
& .\tools\run_uprime_u2_u4_development_tests.ps1 -Lane B0
$env:PYTEST_DISABLE_PLUGIN_AUTOLOAD = "1"
python -m pytest -q tests/test_uprime_u2_u4_development.py
```

It also requires one POSIX/WSL topology probe at the exact T0 commit.  The
local full repository suite is deliberately not registered: this Windows host
has an installed Lean binary, so its conditional legacy tests would invoke
native Lean/RPC in direct conflict with T0's no-native boundary.  A pre-freeze
diagnostic confirmed that conflict by timing out only
`test_kernel_rpc_worker_applies_intro_and_rfl_in_memory` after 180 seconds;
T0's five tests passed, and the diagnostic is neither qualification nor a T0
failure and is not rerun.  Full-suite qualification occurs only in the
Lean-absent hosted candidate and accepted CI environments.

The B0 runner retains its 60-second wall, <=20-second three-times local
qualification threshold, 2-GiB memory cap, and 64-MiB output cap.  No test
function is added, so candidate and accepted full suites must report exactly:

```text
2581 passed, 8 skipped, 161 deselected
```

T0 is accepted only after green candidate CI, exact fast-forward of the
identical SHA/tree from accepted `1d448a2...` through red A2, and distinct
green accepted CI.  Only then may the R2 build ref and E0 source be created.

## 8. Failure, budget, and nonclaims

T0 has at most one calendar day and six active engineer-hours, charged to
A2's remaining 32-hour allowance and the combined 40-hour cap.  The absolute
deadline remains 2026-07-20 13:58:33 JST.

If T0 or its sole correction cannot reach green, the red bootstrap ref/CI is
the terminal record.  The accepted branch remains at `1d448a2...` unless an
immutable accepted-red T0 already exists.  No doc-only recursive closeout is
attempted against a gate known to reject it, and the accepted ref is never
rewound.

T0 reads no protected K1--K4 result and creates no E0 source, fixture,
generator, artifact, EMIT payload, official transport, native Lean/RPC call,
networked semantic child, SSH/remote-CPU run, GPU/CUDA/model-server run,
deployment, or LLM input/result.  LLM knowledge distillation remains last,
only after the theory-driven generator and exact/certified upper pipeline work
independently under separate authority.

The earlier root-level LLM/smoke files remain absent.  Existing user-owned
`docs/external/` dirt remains outside the dedicated worktree and is not staged,
moved, deleted, hashed, imported, hidden, or admitted as evidence.

This bootstrap makes no claim for or against exact quotient coordinates,
worst-case envelopes, MaxEnt, global similarity, locality learning, protected
K-series performance, solve rate, deployment, GPU benefit, or LLM benefit.
