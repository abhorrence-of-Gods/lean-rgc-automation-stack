# U-prime / ODLRQ U2--U4 R3 stage-local re-entry amendment

Date: 2026-07-13 (Asia/Tokyo)

Status: **ONE COMPOSITE CONTROL-PLANE BOOTSTRAP, THEN THE ALREADY FROZEN
E0--I0 MATHEMATICAL ORDER**

Immediate parent: immutable red R2 failure-closeout candidate
`cbc7377c588e024d17438beb83e444c515ff0172`.

Accepted predecessor before R3 activation:
`de8d7e6e3f0cb41b514de25f6b3867bde49033f0`.

R3 is a new authority.  It does not amend, replace, rerun, relabel, or merge
the stopped R2 candidate.  It preserves that exact red commit and Actions run
as its parent, repairs the independently identified control-plane defects in
one bounded bootstrap, and then returns to the unchanged mathematical order
`E0 -> E1 -> E2 -> ME0 -> S0 -> I0`.

## 1. Immutable evidence and scientific interpretation

The accepted predecessor `de8d7e6...` passed candidate CI run `29244989629`,
job `86799628732`, with exactly
`2581 passed, 8 skipped, 161 deselected in 179.26s`.  The identical commit and
tree then passed distinct accepted CI run `29245245989`, job `86800447682`,
with exactly `2581 passed, 8 skipped, 161 deselected in 179.37s`.

R2 E0 development produced an uncommitted three-path implementation whose
focused quotient-generator file passed 30 tests.  The registered E0 runner's
semantic child passed all 35 selected tests, but the runner rejected the
three-times cold-margin rule.  A disposable accepted-base diagnostic then
showed that the inherited 29-test selection itself took 22.76 seconds and
already missed the frozen 20-second threshold.  R2 correctly refused to
raise the wall, delete tests, or add a second runner.

The one-file R2 failure closeout at `cbc7377...` is a sole-parent child of
`de8d7e6...`; its tree is
`917225444c687aeeb801da72545622b95e39cb73`, and it adds only

```text
docs/experiments/uprime_odlrq_u2_u4_development_r2_failure_closeout_2026-07-13.md
```

with Git blob `7a05d80e8854eba26a3a634d345f7fde7a831359`.
Candidate CI run `29248111654`, job `86809828304`, passed Install, Runtime
boundary, and Dead candidate ledger, then ended with exactly

```text
1 failed, 2580 passed, 8 skipped, 161 deselected in 173.00s
```

The sole failure was
`test_u24_b0_anchor_contiguous_budget_and_terminal_topology` at line 821.  The
core topology validator had already extracted the final failure terminal and
correctly returned no completed semantic stage.  Only the outer test then
used a stale positional rule, `two post-T0 rows => the second row is E0`, and
compared the registered failure document with the E0 source allowlist.  This
is a shallow positional-history design error, not a Git shallow-checkout
error, an E0 assertion failure, or a scientific endpoint failure.  R2 stopped
at `U24_CLOSEOUT_CI_BLOCKED` without accepted fast-forward, correction, or
same-SHA rerun.

### 2026-07-13 red-CI clarification retained for future readers

The immutable U05 result-publication commit
`cc91a4181a9f87ec10f11727ed787eb7149f955a` has red Actions run `29166670576`,
job `86580832840`.  Dated audits identify an identity-guard shallow-history
design omission, not a probe, prerequisite, artifact-integrity, or scientific
failure.  The exact scientific candidate
`3bb3408afc50a08307cff2c9b1906a299739dfb5` had green run `29166073728`, job
`86579287017`.  Neither that red badge nor the red R2 closeout badge may be
read as a failed scientific execution.

## 2. Exact supersession and activation

R3 narrowly supersedes only:

1. R2's terminal interpretation for exact commit `cbc7377...`, its exact
   parent/tree/path/blob, and the exact composite child registered below;
2. the outer position-dependent topology assertions, replacing them with
   terminal-first semantic classification; and
3. the E0 runner selection and E0 wall, replacing cumulative pytest
   collection with the fixed stage-local direct-call suite below and changing
   only E0's wall from 60 to 90 seconds.

It does not generically reopen a failure terminal.  A different parent, tree,
failure path/blob, second bootstrap, merge, replacement ref, or another
terminal remains invalid.

The R3 bootstrap candidate branch is
`codex/uprime-u2-u4-development-r3-topology-bootstrap`.  Its initial commit is
an immediate single-parent child of `cbc7377...`.  If the initial candidate
fails for a correction-eligible control implementation defect under section 7,
one immediate control-only correction child may become the bootstrap tip.  R3
activates only after the effective tip (the initial commit or that sole
correction) passes candidate CI.  The accepted branch is then fast-forwarded,
without merge or omission, through

```text
de8d7e6 accepted predecessor
  -> cbc7377 immutable red R2 closeout evidence
  -> R3 composite bootstrap candidate
  -> optional sole bootstrap correction
```

and the identical effective-tip SHA/tree passes a distinct accepted CI.  The
local accepted-ref relationship and the complete ordered prior-stage prefix
are enforced before semantic import by every semantic runner; the candidate
and accepted CI conclusions remain externally audited by their immutable run
and job identifiers.  Before both green runs, no R3 E0 source is created or
imported.

## 3. One composite bootstrap and exact path boundary

The bootstrap changes exactly four paths:

```text
docs/experiments/uprime_odlrq_u2_u4_development_r3_stage_local_reentry_amendment_2026-07-13.md
tests/test_uprime_u2_u4_development.py
tests/uprime_u24_guard.py
tools/run_uprime_u2_u4_development_tests.ps1
```

A document-only child is rejected by the cbc-era positional classifier as a
non-E0 row.  A control-only child has no prior authority and is likewise
invalid.  The same-commit document plus three control files is therefore a
one-time bootstrap necessity, not a reusable `failure -> reopen` pattern.

The bootstrap does not change `.github/workflows/ci.yml`,
`tests/tier_manifest.json`, an E0 source/test, a mathematical fixture, an
artifact, a denial boundary, or any `docs/external/` file.  The workflow blob
remains `0349e9ca864b7e9e25064ab7fab5b6dc0665adee`, and the tier-manifest blob
remains `47997a9bd67f7ac2cf0cd9c7f654d8f069f925d8`.

The new identity oracle re-anchors at exact `cbc7377...`, whose parent, tree,
sole changed path, failure blob, control blobs, base source blobs, and inherited
authority blobs are all frozen.  An empty post-anchor interval is legal only
with the exact four bootstrap paths dirty.  A committed bootstrap is legal
only as the exact four-path sole child.  Later parsing independently extracts
an optional immediate correction and an optional final terminal before it
classifies any semantic row.  No outer assertion may infer a stage from raw
row count.

The existing topology test, without adding a sixth top-level identity test,
must cover at least:

- dirty and committed bootstrap;
- dirty and committed E0;
- immediate `[bootstrap, failure-terminal]`;
- `[bootstrap, correction, failure-terminal]`, the exact R2 regression;
- failure after every semantic prefix;
- success only after all six stages;
- post-terminal, duplicate-bootstrap, merge, skipped-stage, cross-stage
  correction, changed anchor/blob, and unregistered-path rejection.

Every later row retains this amendment, the frozen identity core, guard,
runner, workflow, and authority blobs.  Each semantic row may change only its
frozen stage allowlist; no unchanged base blob is thereby promoted into a
global freeze.  The registered I0 extension may append only its frozen
`test_u24_i0_*` functions outside the hashed identity core.  The runner is not
an I0 source and remains frozen.  Only the optional immediate bootstrap
correction may change the identity core or runner, and either change must also
change the guard so that no stale hash binding can be accepted.

## 4. Single stage-local runner

R3 replaces the existing PowerShell runner in place; it creates no second
runner.  The Python 3.13.7, NumPy 2.3.3, OpenBLAS, single-thread, process,
2-GiB Job-memory, 64-MiB output, denial-guard, receipt, and runtime-identity
contracts are unchanged.  B0 and all non-E0 lane walls remain unchanged.

E0's hard wall is frozen at 90 seconds, so qualification requires an isolated
elapsed time of at most 30 seconds.  Before this amendment, a conservative
exact-node pytest diagnostic of the five identity contracts plus the six
preserved E0 tests passed 11/11 in 21.16 seconds (22.174535 seconds outer).
A separate cold direct-call diagnostic of the strictly larger complete
quotient-generator suite had maximum observed outer time 16.13 seconds over
four completed trials.  These are engineering calibration observations, not
protected endpoints.  The final dirty and clean R3 E0 runner must still prove
the registered margin; a failed margin stops and never raises the wall.

For E0 the runner first installs the same denial guard and directly executes
the five exact identity contracts already used by B0.  It then imports the
quotient-generator test module once and directly executes exactly these nine
unparameterized functions:

```text
test_e0_exact_coordinate_generator_matches_frozen_independent_oracle
test_e0_roundtrip_permutation_cancellation_and_terminal_rows
test_e0_wire_and_source_attacks_fail_closed
test_e0_capability_types_and_later_tiers_are_rejected
test_e0_public_surface_has_no_later_tier_tokens
test_read_only_bundle_cache_is_quarantined_from_mutation_tests
test_e0_scope_fields_fail_closed_at_every_wire_layer
test_e0_signed64_preflight_rejects_before_authority
test_e0_source_type_is_checked_before_attribute_access
```

The wrapper freezes the discovered function set; rejects module/function
marks, `__test__` hiding, explicit pytest plugins, decorators, wrappers,
parameterization, unknown fixtures, skip, xfail, and deselection; and supplies
a fresh `pytest.MonkeyPatch` only to a function whose sole exact parameter is
named `monkeypatch`, undoing it in `finally`.  The E0 receipt must
report exactly 14 passed contracts: five identity plus nine E0.  Full GitHub
CI continues to run the complete repository suite; stage-local selection does
not remove or weaken any full-suite assertion.

Before the semantic module is imported, the runner captures its inspection
primitives and plain-compiles the exact source.  After import it requires the
exact built-in module type and raw module dictionary, and binds every runtime
function's structural code object and globals dictionary to that pre-import
source/module pair.  Rebinding a registered name, filtering `__dict__`, or
supplying the same code under different globals is fail-closed.

The later E1--I0 selections and their walls remain as frozen by the inherited
construction authority.  Their larger thresholds are not recalibrated here.
EMIT and CLOSEOUT semantics are unchanged.

## 5. E0 re-entry and mandatory completion

Only after bootstrap candidate and accepted CI are green, R3 creates
`codex/uprime-u2-u4-development-r3-build` from the accepted R3 bootstrap.  E0
then changes exactly:

```text
lean_rgc/odlrq/quotient_generator.py
lean_rgc/odlrq/__init__.py
tests/test_odlrq_quotient_generator.py
```

The uncommitted R2 worktree is diagnostic source material only.  Its recorded
SHA-256 identities are:

```text
B9E05757BEAD3D0F26182FA93BB7863D737604529D2DDB46F41291E78575DA9C  quotient_generator.py
BEF1F682E388B3361914C31C4ABEB70F7DB34DB3E8DE2711AA22478F7EC157D7  __init__.py
5ACD68F936BE63AEE946714404AF8CFC65DA3B979DEDC0CF694329D7E46C5E99  test_odlrq_quotient_generator.py
```

R3 imports those intended changes only through a fresh diff against the
accepted bootstrap and must close all independently found precommit gaps:

1. generator/row/term kill matrices for omission, alteration, unknown fields,
   and attempted promotion of both mandatory scope fields;
2. `-1` and `2^63` raw-preflight attacks for every E0 index/count field before
   authority access;
3. a source-type-before-attribute trap for builder and parser;
4. independent source-seal reconstruction from literal block/action tables;
5. removal of the unrelated `_fresh_verified` indentation-only diff; and
6. a production construction boundary for which every public/direct
   constructor call fails even if a caller obtains the module-private seal.

The generator becomes `dataclass(frozen=True, init=False)` or an equivalently
strict construction.  Its public `__init__` always fails; only the private
factory may allocate with `object.__new__`, set the three retained fields, and
invoke post-init validation.  Strict public construction and serialization
are the boundary.  Hostile same-process use of `object.__new__`, a private
factory, or monkeypatch remains outside the capability threat model, as it is
for every Python private seal in this stack.

The independent source-seal goldens are frozen:

```text
g0-self     B4E1C7D7BB8FED314196151ABED55B464BA41B793B1F13494CF913FA11BEA87B
g0-move     68CCD648E42F7469CC275B0524FED1D8E79020CD2D6456AE85E528FE67EDD9AF
g0-diamond  624634570FF53154446B46C7945C399BA45146B1DED5DE9B13B62DB83B2BB3A2
g0-members  990CBCEF52531055E512EC8A78F4B0B5084BE1DBC111E9AE622273775BBE605A
```

The independent coordinate-schema digest remains
`9D1F0F6B52CF84E26705C68607405DC1EDEAEA5EBE29D878FD4FEAA5AECD5485`,
and the response-vocabulary digest remains
`3820F0BB274E5EC04F20F014462FBD7CF1D16614B0EE7A85A6E47F6E97743AEF`.
All A2 source, operator, delta, count, work, wire, rational, cap, and tier
requirements remain byte-for-byte authoritative.  Public additions remain
exactly the four A2 names.

E0 focused qualification must report 33 passed tests.  The stage-local runner
must report 14 passed contracts within the 30-second qualification threshold.
Full candidate CI must report exactly
`2590 passed, 8 skipped, 161 deselected`; after exact accepted fast-forward,
distinct accepted CI must report the same count.  No E1 source exists before
both are green.

## 6. Inherited upper-stack order and claims

Except for the explicit R3 deltas above, A2 and the original construction
bundle remain authoritative: exact quotient first, worst-case positive
envelope on quotient coordinates second, finite-horizon/certified binding gate
third, MaxEnt only inside certified support, and finite-approximation-stable
typed global similarity last.  MaxEnt remains model selection and never a
safety source.  Signed cancellation remains before the positive boundary.
Predictive and positive distances remain disjoint.  `alpha` never scales a
generator ray.

The semantic order is fixed:

```text
E0 exact quotient coordinate generator
E1 exact declared-synthetic weighted fiber envelope
E2 finite-horizon cocycle, return-memory bracket, and binding support gate
ME0 MaxEnt nominal model selection inside fixed support
S0 typed global similarity and finite-level remainder
I0 integrated CPU candidate
```

The theory-driven exact/certified pipeline must work independently before any
Lean-oracle production learner, LLM proposal, or LLM knowledge distillation is
registered.  LLM work remains last.  R3 uses local Windows CPU only for
semantic development and does not use native Lean/RPC, official transport,
networked semantic children, SSH, remote CPU, GPU/CUDA, model servers,
deployment, or protected K1--K4 reads.

## 7. Commit, correction, and anti-fractal budget

The R3 refs are frozen:

```text
bootstrap  codex/uprime-u2-u4-development-r3-topology-bootstrap
build      codex/uprime-u2-u4-development-r3-build
closeout   codex/uprime-u2-u4-development-r3-closeout
failure    codex/uprime-u2-u4-development-r3-failure-closeout
accepted   codex/uprime-odlrq-plan
```

The terminal filenames are:

```text
docs/experiments/uprime_odlrq_u2_u4_development_r3_closeout_2026-07-13.md
docs/experiments/uprime_odlrq_u2_u4_development_r3_failure_closeout_2026-07-13.md
```

There is one bootstrap commit, six semantic commits, at most one correction
shared by the bootstrap and all semantic stages, and one terminal commit: at
most nine descendants of `cbc7377...`.  A bootstrap correction is an immediate
single-parent child and may change only the failed bootstrap's three control
files; it cannot change this document, a wall, lane selection, function name,
fixture, formula, endpoint, cap, allowlist, or protected boundary.  If used,
no semantic correction remains.  An initial bootstrap or semantic candidate
CI failure caused by a correction-eligible implementation defect may consume
the sole correction.  A document/specification error, path expansion,
accepted CI failure, red correction, second correction, tenth descendant, or
another bootstrap stops R3.

Before each commit, the intended dirty bytes and the exact path set undergo a
blocker-only adversarial review and the registered Windows lane.  After each
commit, the clean exact SHA passes the lane before push.  Candidate CI must be
green; the identical SHA/tree is fast-forwarded to accepted; distinct accepted
CI must be green before the next source exists.  History is append-only: no
amend, reset, rebase, merge, replacement ref, deletion, force-push, or
same-SHA CI rerun is a repair.

R3 does not add a runner, guard, workflow, ledger, CAS, publisher, recovery
framework, generated-file registry, or per-stage prereg/result triplet.  It
replaces the one existing runner and guard only in the composite bootstrap.
The deadline remains 2026-07-20 13:58:33 JST.  Bootstrap is capped at one day
and six active engineer-hours.  R3 creates no fresh 32-hour allowance; work is
charged only to the auditable remainder of the original 40-hour program cap.

The investigator-owned uncommitted `docs/external/` files remain outside every
R3 input, glob, allowlist, artifact, and commit.  The quarantined root LLM/task
files remain absent and are not recreated.

## 8. Stop rules and nonclaims

The inherited closed labels remain in force.  Bootstrap/control failure is
`U24_B0_IDENTITY_OR_RUNNER_BLOCKED`; E0 failure is
`U24_E0_GENERATOR_BLOCKED`; resource/scope denial is
`U24_RESOURCE_OR_SCOPE_BLOCKED`; red terminal CI is
`U24_CLOSEOUT_CI_BLOCKED`.  The R2 document's
`U24_E0_GENERATOR_BLOCKED` and its final `U24_CLOSEOUT_CI_BLOCKED` evidence are
preserved without relabelling.

An eligible initial bootstrap or semantic candidate CI failure may use the
single correction defined in section 7 if it remains unused.  If that
correction fails, or if an ineligible failure, accepted CI failure, second
failure after the correction budget is spent, or terminal CI failure occurs,
R3 stops on its immutable ref without further repair or recursive closeout.
Accepted is never rewound.  A failure terminal is legal after zero or more
semantic stages; a success terminal is legal only after all six.

No R3 engineering failure is evidence that the upper mathematical program is
impossible.  No outcome here claims protected K1--K4 performance,
same-family D4 rank, production Lean locality, complete all-germ quotient,
production hard envelope, MaxEnt safety, global Lean similarity, learner
improvement, solve rate, deployment, remote/GPU benefit, or LLM benefit.
