# Research History Notes

This directory preserves the `LEAN_RGC_V*_NOTES.md` files from the research
phase. They are useful as design history and regression context, but they are
not the production README or API contract.

The production-facing docs live one level up in `docs/`.

